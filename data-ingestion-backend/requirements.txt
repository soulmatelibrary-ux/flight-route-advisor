# 버전 고정 근거: data-ingestion-backend/docs/기술스택_결정.md §6
# --- 백엔드 ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.19
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
python-dotenv==1.0.1

# --- 스킬 실행 의존성 (SOURCE_PROJECT_ROOT/00_시작안내/requirements.txt와 동일 버전 고정) ---
pandas==2.3.3
openpyxl==3.1.5
shapely==2.1.2

# --- 이 백엔드 자체의 업로드 포맷 확장(.xls 지원, 스킬 자체는 요구하지 않음) ---
# 스킬은 .xlsx/.csv만 읽으므로(무수정), .xls 업로드는 workspace_builder가 .xlsx로 변환해
# 배치한다 — xlrd는 그 변환 단계(및 raw 계층 원본 재읽기)에서만 쓰인다.
xlrd==2.0.1
