# 09. 문서 리뷰 노트 (문제점·개선점 로그)

- 문서 버전: 1.0
- 작성일: 2026-07-21
- 목적: 설계 문서 세트를 리뷰해 발견한 문제점과 반영한 개선점을 추적한다. 새 리뷰 발견 시 이 표에 추가한다.

## 1. 반영 완료 (이번 라운드)

| # | 문제점 | 개선(반영 위치) | 상태 |
|---|---|---|---|
| R1 | 소유권 표현이 "다른/별도 AI가 ingestion 담당"으로 남아 단일폴더 순차개발과 불일치 | "이 저장소에서 Stage 0로 먼저 개발" 로 통일 (00, README, 07) | ✅ |
| R2 | 자기완결성 갭 — skills·원본데이터·사전빌드_JSON·기상서버·원본 문서가 외부에 있는데 경로 규약 부재 | env 루트(`SOURCE_PROJECT_ROOT`/`PORTING_PACKAGE_ROOT`)와 자산 인벤토리 신설 (08, CLAUDE.md §0.1) | ✅ |
| R3 | 로컬 pgsql 핸드오프 불명확 — advisor가 어디서 데이터를 얻는지 | Stage 0가 채운 동일 로컬 pgsql 소비 명문화 (08 §3, 02) | ✅ |
| R4 | 시큐어코딩 규약 부재 | 06 §8 신설(입력검증·SQLi·경로주입·비밀정보·최소권한·CORS·의존성·오류처리) + CLAUDE.md §3 + 07 게이트 | ✅ |
| R5 | 완성본 HTML(15MB) 취급 미규정 | `.gitignore`·정답지·직접 Read 금지 (06 §7, 07, 08) | ✅ |
| R6 | ODR2 배치 트리거가 "협의"로 모호 | "Stage 0 SUCCESS run 후 로컬 pgsql에서 집계" 로 정리 (02 §4.3, 08) | ✅ |
| R7 | 코드 리뷰 방식 미규정 | 리뷰에이전트 게이트(기능·논리·예외·보안) 의무화 (CLAUDE.md §4, 07 공통 게이트) | ✅ |
| R8 | 물리 컬럼명 "미확정" 서술이 낡음(§9 확정본 존재) | 확정본 반영·정렬키 명시 (02 §5, 03) | ✅ (이전 라운드) |
| R9 | 문서 버전/작성일 헤더 불일치 | docs/00~08 헤더 통일 | ✅ |
| R10 | 두 "result" 폴더(백데이터 입력 vs 완료검증 출력) 혼동 위험 | 명시적 구분 (CLAUDE.md §0.1·§8, 08 §1, result/README) | ✅ |

## 2. 개발 중 확정할 열린 이슈 (문서에 명시, 코드 단계 결정)

| # | 이슈 | 참조 |
|---|---|---|
| O1 | ODR2 집계 방식(materialized view vs 배치 아티팩트)·자동 트리거 구현 | [02](./02-db-integration.md) §4.3, §8 |
| O2 | 여러 달 데이터가 다른 run으로 적재될 때 "기간 스냅샷" 선택 규칙 | [02](./02-db-integration.md) §3.2 |
| O3 | 참조 지오메트리 2단계 타일화 여부(성능) | [01](./01-architecture.md) §4, [05](./05-mvp-scope.md) |
| O4 | 통합데이터·영향상세 테이블 완성 시점 → 흐름관리 영향(3단계) 착수 | [02](./02-db-integration.md) §2 |
| O5 | env 루트 실제 경로 확인(백데이터 `project/result/` 접근권) | [08](./08-setup-and-dev-order.md) §1 |

## 3. 리뷰에이전트 문서 리뷰 결과 (1회차, 2026-07-21)

senior-code-reviewer가 전체 문서 세트를 실측 대조 리뷰. 총평: **조건부 가능**(H1·H2 해결 시 "가능"). 발견/조치:

| # | 심각도 | 발견 | 조치 |
|---|---|---|---|
| H1 | High | Stage 0 코드 위치가 `_MIRROR.md`(원본) vs `07`(이 저장소) vs `CLAUDE.md`(읽기전용) 3-way 모순 | ✅ `_MIRROR.md` 정정, `08 §2.1` 코드 위치표 신설, `01 §5`·`05 §1`·`I1` db 소유권 정합 |
| H2 | High | `02 §3.2` "최신 run 1개" SQL을 다개월 ODR2 집계에 쓰면 과거 데이터 조용히 누락 | ✅ §3.2 경고 추가, `07` Stage 1 착수 전 필수 확정 체크박스로 승격 |
| M1 | Med | rate limiting 부재(조직 정책 위반) | ✅ `06 §8` 요청량 제한 행 + `07` 공통 게이트 |
| M2 | Med | advisor GRANT 범위 불명확(raw_* 노출 위험) | ✅ `02 §6` 테이블 명시 GRANT 예시(processed_*만) |
| M3 | Med | 완성본 HTML↔신규 프론트 값 비교 방법 부재 | ⏳ 추적 — Stage 2 착수 시 "완성본 const를 Node 정규식 추출→JSON diff" 골격 `04 §8`에 추가 예정 |
| M4 | Med | Docker db 서비스 소유권 문서마다 상이 | ✅ H1과 함께 "이 저장소 로컬 db"로 통일 |
| M5 | Med | FIR ±360 좌표 vs bbox 파라미터 좌표계 정규화 규칙 부재 | ⏳ 추적 — Stage 1 `/api/reference` bbox 구현 시 `03 §3`에 정규화 규칙 명시 예정 |
| L1 | Low | "경로 3,083" 수치 출처 각주 부재 | ⏳ 추적 |
| L2 | Low | `01 §2.4` SUAS/SID-STAR 단계 구분 미표기 | ⏳ 추적(“(2단계)” 주석) |
| L3 | Low | XLSX 압축폭탄(decompression bomb) 제한 부재 | ✅ `06 §8` 업로드 폭탄 행 추가 |
| L4 | Low | ODR2 트리거가 acdm/fois run에도 오인 발동 소지 | ✅ `02 §4.3` `run_type='flight_data'` 한정 명시 |

> ⏳ 추적 항목(M3·M5·L2)은 해당 Stage 착수 시점에 반영한다(지금 반영하면 아직 구현 전이라 과설계). 개발 블로커(H1·H2)와 보안 필수(M1·M2·L3)는 즉시 반영 완료. L1은 2회차에서 재정정(아래).

## 4. 리뷰에이전트 문서 리뷰 결과 (2회차, 2026-07-21)

senior-code-reviewer가 docs/10-ui-and-realtime.md·design-proposal.html 추가 이후 전체 문서셋을 재대조. 1회차 조치(H1·H2·M1·M2·M4·L3·L4)는 전부 실제 반영 확인됨. 신규 발견 및 조치:

| # | 심각도 | 발견 | 조치 |
|---|---|---|---|
| L1(재정정) | Low | "각주 누락"이 아니라 **존재하지 않는 근거(06 §7)를 인용하는 오류** — 02 §3.2·05 B6·07이 "경로 3,083"을 06 §7 인용으로 표기했으나 원래 06 §7엔 그 수치가 없었음 | ✅ `06 §7`에 "경로 3,083"(원본 `06_재현가이드`·`08_임베드데이터_스키마` §"규모" 실측 확인) 추가 |
| R2-1 | High | `04 §3.1`(초기 부트: 참조 레이어 8종 무조건 병렬 프리페치)이 `10 §2`(기본=결정 포커스, 전세계는 온디맨드)와 정면 충돌. 기존 §5의 보정 메모가 §3.1 본문을 고치지 못해 문서 내부 모순 | ✅ `04 §3.1` 부트 순서 전면 재작성(결정 포커스 최소 부트→OD 선택 시 enroute_firs만 조회→모드 전환 시 전세계 벌크), `§5`·`§6`·`§8` 연동 수정 |
| R2-2 | Medium | `05`/`07` Stage2 작업분해에 `10`이 확정한 3뷰 모드 토글 구현 항목이 없음 | ✅ `05 §2.2` F9(뷰모드 토글)·F10(미니맵) 추가, DoD(§2.4) 갱신, `07` Stage2에 동일 반영 |
| R2-3 | Medium | `03 §4.2~4.4`(ACDM/FOIS/흐름관리를 MVP로 명세) vs `05 §2.1/§3`(같은 기능을 2단계로 분류) — Stage1 범위 자체가 문서 간 모순 | ✅ **사용자 결정: 2단계로 연기, 03/07을 05에 맞춤.** `03 §4.2~4.4`에 "(2단계)" 표기 + MVP는 §4.1만이라는 안내 추가, `07` Stage1 체크리스트에서 3개 엔드포인트 제거해 "향후 확장·2단계"로 이동, `05 §3`에 흐름관리(자체 전처리분) 2단계 행 추가 |
| R2-4 | Low | `10 §2.5` 위험기상(SIGMET/PIREP) 근거가 "기상서버 제공"이라 했지만 `03 §5`엔 해당 엔드포인트 언급 없음 — 실측 확인 결과 원본 `문서/07_기상MCP서버.md`에 `isigmet`·`pirep` 엔드포인트 **실재**(신규 개발 불필요) | ✅ 근거 확인됨. `10 §2.5`에 단계 열 추가해 MVP/2단계 명시 |
| R2-5 | Low | `10 §2.5`·design-proposal.html §03이 CAT 시어/상층풍 오버레이·★추천고도를 "기본:항상"으로 표기하나 `00 §3`(Out of Scope)·`05 §3`(2단계)은 이를 04-E 2단계 기능으로 명시 — MVP 화면에 아직 없는 기능이 상시 레이어로 잘못 표기됨 | ✅ `10 §2.5` 표에 "단계" 열 추가, ①행을 2단계로 정정, `10 §2.2` 레이아웃 다이어그램에 MVP/2단계 항목별 주석 추가, design-proposal.html §03 표·캡션도 동일하게 정정(우측 패널·정시% 표기는 "2단계 이후 목표 화면"임을 명시) |

> 3회차부터는 R2-3(ACDM/FOIS/흐름관리 2단계 이관)에 따라 Stage 1 작업량이 축소된 것을 반영해 `07-checklist.md` 진행 판단 시 참고할 것.

## 5. 사용자 제보 리뷰 대조 (3회차, 2026-07-21)

사용자가 외부 리뷰 결과(문서 충돌 4건)를 제보. Explore로 원문 대조 검증 후 조치:

| # | 심각도 | 발견 | 판정 | 조치 |
|---|---|---|---|---|
| R3-1 | High(제보) → 재판정 Low | `00 §3`·`05 §4` "전처리 파이프라인/적재 자체는 비범위" vs `00 §1`·`07`·`08 §2.1` "Stage 0는 이 저장소에서 개발" | **실제 모순 아님** — "스킬 로직 자체"(비범위)와 "그 로직을 호출하는 Stage 0 적재 백엔드 앱"(범위 안)은 서로 다른 대상. 다만 문구가 이를 구분하지 않아 오독 소지 있었음(사용자 확인: 문구만 정밀화) | ✅ `00 §3`·`05 §4`에 "스킬 로직 자체(비범위)" vs "Stage 0 적재 백엔드 앱(범위 안)" 명시적으로 분리 |
| R3-2 | Medium | `README.md` 불변 원칙(구 L89) "여기의 `data-ingestion-backend/`는 읽기용 미러다"가 상단 설명(구 L30, `docs/`만 미러)·`_MIRROR.md`·`08 §2.1`(app/docker는 신규 코드)과 충돌 — README 내부 자기모순 | **CONFIRMED** | ✅ 불변 원칙 항목을 "`data-ingestion-backend/docs/`만 읽기용 미러, `app/·docker/`는 Stage 0 신규 코드"로 정정 |
| R3-3 | Medium | README "폴더 구성"·"현재 상태"가 `docs/00~07`만 언급, 같은 문서의 "읽는 순서" 표는 이미 08·09·10 포함 — 섹션 간 불일치 | **CONFIRMED** | ✅ "폴더 구성"에 08~10 및 Phase 1 산출물(`backend/`,`docker/`,`.env.example`,`config.example.json`) 추가, "현재 상태"를 docs/00~10 + Phase 1 착수됨으로 갱신 |
| R3-4 | Low | `00 §7`(DoD)이 `[01]~[06]` 링크 유효성만 조건, 07~10 정합성 점검 없음 | **CONFIRMED** | ✅ `00 §7`에 "문서 간 정합성 — 09-review-notes 라운드별 기록, 신규 문서 추가 시 기존 문서와 재검증" 조건 추가 |

> R3-1은 "Stage 0을 이 저장소에서 할지 말지"의 실질적 재논의가 아니라 **비범위 대상을 정밀하게 특정**하는 조치로 마무리됨(사용자 확정). Phase 1 스캐폴딩(backend/app/config.py, docker/, db 컨테이너)은 이번 라운드에서 그대로 유지하기로 확정(추가 코딩은 사용자 명시적 지시 시에만 진행).
