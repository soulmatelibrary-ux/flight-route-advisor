# 07. 구현 체크리스트 (순차 개발 진행 기준점)

- 문서 버전: 2.0
- 작성일: 2026-07-21
- 관련 문서: [00-plan](./00-plan.md), [02-db-integration](./02-db-integration.md), [03-backend-api](./03-backend-api.md), [04-frontend-migration](./04-frontend-migration.md), [05-mvp-scope](./05-mvp-scope.md), [06-conventions](./06-conventions.md), [08-setup-and-dev-order](./08-setup-and-dev-order.md)

> **이 문서가 진행 기준점이다.** 개발 중단 후 재개 시: ① 여기 체크 상태 확인 → ② 마지막 완료 Stage/항목의 관련 문서 재확인 → ③ **다음 미체크 항목부터** 이어서.
> 순서: **Stage 0 전처리 적재 → Stage 1 advisor 백엔드 → Stage 2 프론트**. 이전 Stage의 게이트를 통과해야 다음 Stage가 의미를 가진다.

## 공통 게이트 (모든 Stage·모든 항목에 적용)

- [ ] 하드코딩 없음: URL/포트/임계/색상/컬럼명/경로/자격증명이 env·config·`column_map`에만 존재([06 §1](./06-conventions.md))
- [ ] **시큐어코딩**([06 시큐어코딩 절](./06-conventions.md)): 입력검증·파라미터라이즈드 쿼리·경로주입 차단·비밀정보 비커밋·최소권한 role(테이블 명시 GRANT)·CORS·**요청량 제한(rate limit)**·의존성 고정·오류 비노출
- [ ] **리뷰에이전트 통과**: 항목 완료 전 `code-reviewer`/`senior-code-reviewer`로 ①기능 ②논리 ③예외처리 ④보안 취약점 점검 → 지적 수정 후 체크, 요약 보고

---

## Phase 0 — 문서화 (완료)

- [x] docs/00~08 + CLAUDE.md + result/ + data-ingestion-backend 미러 작성
- [x] docs/09-review-notes.md(문서 리뷰 로그)
- [x] 선행조건 확인: env 루트(`SOURCE_PROJECT_ROOT`/`PORTING_PACKAGE_ROOT`) 접근 가능, 로컬 pgsql 준비([08](./08-setup-and-dev-order.md))

## Phase 1 — 로컬 환경

- [x] `backend/app/config.py` — env→Settings(하드코딩 0), `.env.example`, `config.example.json`
- [x] `docker/docker-compose.yml` — `db(postgres:16)` + `route-api` + `weather`(기존 기상서버)
- [x] `.gitignore`(`.env`, 참조 아티팩트, 15MB HTML 등)
- [x] `docker compose up db` 후 접속 스모크 — `docker compose --env-file .env -f docker/docker-compose.yml up -d db` healthy 확인, `psql`로 `aviation` DB 접속·테이블 18종 조회·`advisor_readonly` role 존재 확인

---

## Stage 0 — 전처리 적재 백엔드 (로컬 pgsql)

> 근거: [data-ingestion-backend/docs](../data-ingestion-backend/docs/) (작업계획서·DB스키마§9·스킬연동_레퍼런스·체크리스트). 코드는 이 저장소에서 개발, 스킬·원본데이터는 `SOURCE_PROJECT_ROOT`(env) 참조.

- [x] Alembic 마이그레이션: `ingestion_runs`·`ingestion_run_files`·`ingestion_logs` (raw_files 포함 — FK 의존 순서상 이 리비전에 통합, `data-ingestion-backend/alembic/versions/82f408ef63f3_*`)
- [x] `raw_files` + `raw_*_rows` 7종 (`68b1cff4780c_*`, `app/db/column_map.py` 단일출처)
- [x] `processed_*` 6종 (물리 컬럼 = [DB스키마 §9](../data-ingestion-backend/docs/DB스키마.md), `ac001a0ab3d9_*`) + 최소권한 role `advisor_readonly`(`360c8b394406_*`)
- [x] 스킬 연동: `registry`(SkillDescriptor)·`workspace_builder`·`skill_runner`(subprocess) — 스킬 코드 무수정(sha256 대조 확인), `flow-management`는 `--no-integrate`. 4종 스킬 모두 실제 원본 샘플로 subprocess 호출 성공 확인(ACDM·FOIS는 출발/도착 양방향 파일이 동시에 있어야 하는 실제 제약을 발견해 반영)
- [x] raw/processed 적재 로더(run_id 태깅, 트랜잭션 분리) — `app/ingestion/loaders.py`(`create_run`/`finish_run`/`load_raw`/`load_processed`), run 단위 트랜잭션(raw/processed 별도), 원본은 서버생성 UUID 파일명으로 `UPLOAD_DIR` 아카이브. 4종 스킬 전체 데이터셋으로 raw+processed 적재 후 (a) **원본 파일 재읽기 값 ↔ raw_*_rows(typed+extra_columns) 완전 일치**, (b) **processed DB 값 ↔ 스킬 산출 CSV 값 완전 일치** 둘 다 pandas로 확인(리포트: [result/phase8-baseline-verification-2026-07-21.md](../result/phase8-baseline-verification-2026-07-21.md) §3)
- [x] 업로드 API/폼 + 로그 조회 페이지(상태 뱃지·검증 경고) — `app/main.py` + `app/routers/{upload,runs}.py` + `app/templates/{upload,runs,run_detail}.html`. `app/ingestion/pipeline.py`(run_ingestion, QUEUED→RUNNING→SUCCESS|VALIDATION_FAILED|FAILED, 프로세스 전역 락으로 순차 실행), idempotency_key 재시도 방지, XLSX 압축폭탄·업로드 용량(개별+합산) 검증. uvicorn 실제 기동 후 curl로 4종 스킬 전부(성공/400 검증실패 2종/스킬 런타임 실패/idempotency 중복방지/CSV 다운로드) HTTP 종단 확인
- [x] **완료 기준**: 원본 폴더 처리 결과가 스킬 직접 실행 기준선과 동일(작업계획서 완료기준, §10.2). 4종 원본 **전체 데이터셋**(부분집합 아님)을 ① 스킬 직접 CLI 재실행(ACDM은 기존 outputs/acdm_timeline_fixed 사용 금지 원칙대로 새로 생성)한 독립 기준선과, ② 백엔드로 처리한 결과(DB 값·백엔드 CSV) 사이에서 processed_* 6개 테이블 전부 행수·전체 컬럼 값 완전 일치 확인(불일치 0건). 리포트: [result/phase8-baseline-verification-2026-07-21.md](../result/phase8-baseline-verification-2026-07-21.md). 과정에서 macOS 파일명 유니코드 정규화(NFD/NFC) 이슈를 발견했으며 백엔드 자체에는 영향 없음(리포트 §1 참고)
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — 이번 라운드까지의 전체 산출물(registry/workspace_builder/skill_runner/db스키마/loaders/업로드API/pipeline) 리뷰 3회 통과·지적사항 전부 수정 완료(idempotency 경합, XLSX 압축폭탄 메타데이터 신뢰 문제, 업로드 용량 검증 시점 등 포함)
- [x] **(체크리스트 외 추가) 업로드 포맷 확장**: xlsx 외 xls/csv도 4종 모두 허용 — 스킬 코드는 무수정 원칙이라, 스킬이 직접 못 읽는 확장자(flight_data/fois/flow_management의 xls/csv, ACDM의 xls)는 `workspace_builder._convert_to_xlsx`가 헤더 해석 없이 셀 그리드 그대로 xlsx 컨테이너로 옮겨 담아 배치하고(스킬은 그 결과만 읽음), raw 계층(`loaders._read_raw_dataframe`)은 항상 사용자가 올린 원본 그대로를 읽어 원본 충실도를 유지한다. `xlrd` 의존성 추가(.xls 읽기). 실측 샘플을 csv로 변환해 raw 재읽기 값 일치 확인 + flow_management 1건은 csv 업로드로, `.xls` 픽스처는 실제 `POST /uploads` HTTP 종단으로 전체 파이프라인(workspace 변환→스킬 실행→검증→적재) 실행해 SUCCESS·값 일치 확인(CSV는 시트 개념이 없어 `원본시트` 메타 컬럼만 파일명 기반 값으로 대체됨 — 데이터 손실 아님). 리뷰에이전트 지적사항 수정: 변환 실패(인코딩 등) 시 처리되지 않은 500 대신 `WorkspaceBuildError`로 통일, 배치 파일명 확장자 소문자 정규화(대문자 확장자가 케이스 센시티브 파일시스템에서 스킬 glob에 안 걸리는 문제)
- [x] **(체크리스트 외 추가) run 삭제·재입력 루틴**: 기존에는 삭제 경로가 없어 같은 날짜를 재업로드하면 append-only 특성상 중복이 쌓였고, 완전동일 파일 재업로드는 처리되지 않은 예외(500)로 이어지는 버그도 있었다. `7c1e4a9b2f6d_*` 마이그레이션으로 `ingestion_runs.status`에 `DELETED` 추가 + `deleted_at`/`deleted_by` 컬럼 추가, `loaders.delete_run`(terminal 상태만 삭제 가능, raw_*/processed_*/링크/아카이브 파일 삭제, run 메타데이터 자체는 감사 기록으로 보존 — 사용자 확정 정책), `POST /runs/{run_id}/delete` + `run_detail.html` 삭제 폼. 함께 `load_raw`의 미처리 IntegrityError(중복 파일 재업로드 시 500)를 명확한 400 메시지로 수정. delete_run 가드(이미 DELETED/QUEUED/존재하지 않는 run 거부, 다른 run 데이터 비영향) HTTP 종단(curl) 확인. 리뷰에이전트 지적사항 수정: 이 앱 전체에 인증이 없어 삭제처럼 되돌릴 수 없는 작업만 별도 게이트가 필요하다는 지적에 따라 `INGESTION_DELETE_TOKEN`(미설정 시 기능 자체 비활성화, fail-closed) + `hmac.compare_digest` 검증 추가
- [x] **(체크리스트 외 추가) 적재 데이터 조회 웹페이지 + CSV 다운로드 + 실행 스크립트**: DataGrip 등 외부 DB 클라이언트 없이 이 앱만으로 적재 결과를 확인할 수 있도록 `GET /runs/{run_id}/data/{table_name}`(페이지네이션, run_type별 화이트리스트로 테이블 접근 제한) + `GET /runs/{run_id}/data/{table_name}/download.csv`(청크 스트리밍, BOM 포함) 추가. `data-ingestion-backend/start.sh`(루트 `.env`의 `INGESTION_PORT` 사용, 하드코딩 없음)로 로컬 실행 표준화. uvicorn 실제 기동 후 curl로 페이지네이션·테이블 화이트리스트(다른 run_type 테이블 접근 시 404)·CSV 다운로드(processed/raw 둘 다, extra_columns 포함)·삭제 플로우(토큰 누락/오류/정상 3가지) 전부 HTTP 종단 확인
- [x] **(2026-07-21 후속 리뷰 2차 — 데이터 조회 페이지·CSV export 리뷰)** `senior-code-reviewer`가 47번 항목(조회/다운로드)을 리뷰. 지적사항 2건 수정: ① `download_data_csv`의 `extra_columns`가 `str(dict)`(파이썬 repr, 유효한 JSON 아님)로 나가던 것을 `json.dumps(..., ensure_ascii=False)`로 수정. ② 청크마다 새 커넥션을 열어 내보내는 도중 그 run이 삭제되면 CSV가 조용히 잘리는 문제 — `REPEATABLE READ` 트랜잭션 하나로 전체 export를 묶어(스냅샷 고정) 해결, 실제로 "export 시작 → 다른 트랜잭션에서 delete_run 커밋 → export 계속" 순서로 재현해 잘리지 않음을 확인. 화이트리스트(다른 run_type 테이블 접근 차단)·`INGESTION_DELETE_TOKEN` 로직·start.sh의 `.env` 소싱 안전성은 문제없음으로 확인됨(리뷰 보고서)
- [x] **(2026-07-21 후속 리뷰 잔여 3건 처리)** 48번(1차 후속 리뷰)의 나머지 지적사항: hmac TypeError(비ASCII 토큰 입력 시 500) → `admin_token.encode()`/`delete_token.encode()`로 bytes 비교하도록 수정, 실제로 한글 토큰 입력 시 500이 아니라 403 반환 확인. OFFSET 성능 지적 → `download_data_csv`(전체 export, 실제로 비용이 문제되는 경로)만 커서 기반(`id > last_id`)으로 전환, 인터랙티브 조회(`run_data`)는 임의 페이지 이동이 필요해 OFFSET 유지(의도적 — 코드 주석에 근거 남김). FOIS CSV `skiprows=1` 가정(49번)은 실제 독립 FOIS CSV 샘플이 없어 검증 불가 — 코드 주석으로 명시적 한계 문서화만 하고 미해결로 남김(추후 실제 샘플 확보 시 처리)
- [x] **(체크리스트 외 추가) 테이블 전체 조회 페이지 + 웹 UI 스타일 정리**: run 하나에 매이지 않고 raw_*_rows/processed_* 13종 전체를 훑어볼 수 있는 `GET /tables`(테이블별 총 행수) + `GET /tables/{table_name}`(페이지네이션, 선택적 `run_id` 필터) + `GET /tables/{table_name}/download.csv`(REPEATABLE READ+커서, run_data와 동일 패턴) 추가(`app/routers/tables.py`). 모든 페이지에 공통 상단 탭 네비게이션(`_nav.html`, 현재 위치 강조) 적용, `style.css` 전면 개편(카드 레이아웃·sticky 헤더·줄무늬 테이블·뱃지/버튼 톤 정리). uvicorn 기동 후 curl로 `/tables` 목록·개별 테이블 조회·알 수 없는 테이블 404·`run_id` 필터·CSV(run_id 컬럼 포함) 전부 HTTP 종단 확인, 기존 업로드/처리로그/run상세 페이지도 신규 스타일 적용 후 정상 동작 재확인
- [x] **(회귀 발견·수정 확인) `column_map.py`의 PROCESSED_FLOW_MANAGEMENT 일시적 회귀**: 이번 CSV/CP949 종단 검증 중 flow_management 업로드가 매번 `CSV에 없는 필수 컬럼: ['기상세부분류']`로 실패하는 것을 발견 — 실제 스킬 산출 CSV 헤더를 다시 덤프해 대조한 결과 "원본파일"/"원본시트"/"기상대분류"가 맞고(DB 실제 물리 컬럼 `source_file`/`source_sheet`/`wx_category`와도 일치), "기상세부분류"·누락된 원본파일/원본시트는 잘못된 회귀였음을 재확인. 수정 후 CP949 CSV 업로드 HTTP 종단 재실행(SUCCESS·378행) + 기존 baseline run(6c0183d3-2ad7-4ecc-8d90-da2d550c0f57) 조회로 스키마 일관성 재확인
- [x] **(사용자 요청) `data-ingestion-backend` ↔ `project/result/데이터전처리기술이식` 데이터 동일성 비교 리뷰**: "DB/백엔드 CSV가 원본 스킬 산출물과 데이터가 동일해야 하고, 자료 누적 시 성능·예외처리도 검증해달라"는 요청으로 `app/db/column_map.py`(PROCESSED_COLUMNS 6종) 논리컬럼을 실제 스킬 산출 CSV 헤더와 전수 대조.
  - flight_data/acdm 출발·도착/fois 출발·도착 5종은 컬럼명·순서까지 완전 일치(정적 `outputs/` 샘플 기준).
  - **processed_flow_management만 최초 불일치로 보였으나 오탐이었음**: `SOURCE_PROJECT_ROOT/outputs/흐름관리일지_전처리_*.csv`(정적 샘플, 7/19일자)엔 `원본파일`/`원본시트`가 없고 `기상세부분류`였는데, 이 샘플이 오래된 스킬 실행분이었다. 스킬(`run_flow_management_preprocessing.py`)을 스크래치 디렉터리에 직접 재실행해 만든 진짜 최신 기준선은 32열(원본파일·원본시트·기상대분류 포함)로 `column_map.py`(수정 전 원본)와 정확히 일치 + 기존 [result/phase8-baseline-verification-2026-07-21.md](../result/phase8-baseline-verification-2026-07-21.md)의 "완전 일치" 판정과도 일치. 사용자 지시로 한 차례 `column_map.py`를 정적 샘플 기준으로 고쳤다가, 이 재검증으로 즉시 원상복구함(코드 값 변경 없음).
  - **실제 남아있던 문제는 `docs/DB스키마.md` §4·§9.6 쪽**: 이 문서가 위 스키마 갱신(원본파일/원본시트 추가, 기상세부분류→기상대분류/`wx_detail`→`wx_category`)을 반영하지 못해 코드보다 뒤처져 있었음 — 실제 코드/DB/기준선에 맞게 갱신.
  - **교훈**: `project/result/…/outputs/`의 정적 샘플 CSV는 스킬 재실행 없이 비교 근거로 쓰면 안 됨(ACDM `outputs/acdm_timeline_fixed`가 이미 같은 이유로 금지된 전례를 이번엔 flow_management에도 적용해야 했음). 데이터 누적 성능·예외처리는 지난 라운드(48·49번) 검토·수정 내용 그대로이며 이번 비교에서 새로 발견된 성능 이슈는 없음.
- [x] **(사용자 요청) 스킬 제외 행수 run 로그 기록**: raw/processed 행수가 다른 이유(ACDM `검토필요`, 흐름관리일지 `Seq` 비숫자)를 매번 두 테이블을 비교해 알아내지 않아도 되도록 `pipeline._log_exclusions()` 추가 — ACDM은 스킬 stdout 리포트가 이미 계산해 주는 `{direction}_excluded_review_rows`를 그대로 로그에 옮기고(스킬을 블랙박스로 다루는 원칙상 재계산하지 않음), 흐름관리일지는 스킬 리포트에 제외 전 행수가 없어 우리가 이미 적재해 둔 raw_flow_management_rows 행수와 스킬이 보고한 이벤트 행수의 차로 역산한다. 제외 0건이면 INFO, 1건 이상이면 WARN으로 `ingestion_logs`에 남아 run 상세 페이지에서 바로 보인다. flow_management는 실제 `POST /uploads` HTTP 업로드로 종단 검증(WARN 로그 "Seq가 숫자가 아닌 3행 제외 (원본 381 → 이벤트 378)" 확인), ACDM은 실제 데이터셋이 이미 전량 적재돼 있어 동일 파일 재업로드가 중복 방지 제약에 막히므로 스킬의 실제 stdout 리포트 형태를 그대로 흉내낸 값으로 로그 포맷팅 로직만 별도 검증
- [x] **(체크리스트 외 추가) `/tables` 탭 UI를 fetch 기반 단일 페이지로 전환**: 테이블 이름을 눌러도 페이지 이동 없이 `#table-content`에 바로 표시되도록 `tables.js` + `_table_data_content.html`(본문 조각, `table_data.html`과 공유) + `GET /tables/{table}?partial=1`(조각만 반환) 추가. 라우터가 `partial` 파라미터를 받고도 항상 전체 페이지를 반환하던 미완성 상태를 발견해 `partial=1`일 때 `_table_data_content.html`만 렌더링하도록 수정 — 실제로 fetch 응답에 `<html>` 태그가 없는 순수 조각인지, 일반 요청은 여전히 전체 페이지인지 curl로 확인

## Stage 1 — advisor 백엔드 (FastAPI, 읽기 전용)

> Stage 0가 채운 동일 로컬 pgsql `processed_*` 소비. 근거: [02](./02-db-integration.md)·[03](./03-backend-api.md).

- [x] **[착수 전 필수 확정 — H2/O2]** append-only 다회차 적재 시 "어느 run을 집계·조회 대상으로 삼는가" 정책 확정 — **일자별 최신 run 우선**(각 테이블 날짜 컬럼 값별로 그 값을 포함하는 SUCCESS run 중 `finished_at` 최신 run의 행만 채택, `ROW_NUMBER() OVER (PARTITION BY 날짜컬럼 ORDER BY finished_at DESC)`). 사용자 확정(2026-07-22), 상세 [02 §3.2](./02-db-integration.md). 실제 적재 상태 확인: 현재 4개 run_type 모두 SUCCESS run 1개뿐(flight_data는 2026-01 전체 84,520행) — 다회차 상황은 아직 없으나 향후 재업로드·월별 추가 대비 규칙 확정
- [x] `db/session.py`(읽기전용 엔진, 로컬↔Supabase 분기), `db/tables.py`, `db/column_map.py`([DB스키마 §9] 단일출처) — `ADVISOR_DATABASE_URL`(신규 env, `advisor_readonly` role 전용)로 분리(기존 `DATABASE_URL`은 Stage 0 쓰기 role과 공유돼 있어 최소권한 위반 소지 발견·수정, 마이그레이션 `9e2a5d7c1b4f`로 `ingestion_runs` 컬럼단위 GRANT 보강). `db/tables.py`는 `include_columns`+`resolve_fks=False`로 리플렉션을 화이트리스트에 강제(FK 자동 리플렉션이 `include_columns`를 무시하는 함정 실측 발견·수정). 실제 DB로 리플렉션 성공 + `ingestion_runs` 5컬럼만 노출 확인
- [x] `queries/latest_run.py` — 최신 SUCCESS run 규약(단순 SELECT 금지, [02 §3](./02-db-integration.md)). "일자별 최신 run 우선" 2단계 윈도우(날짜·run_id distinct → 날짜별 승자 run_id → 원 테이블 재조인)로 구현 — 원본 행에 바로 ROW_NUMBER를 걸면 하루 여러 행이 1행으로 뭉개지는 함정을 실측으로 발견해 회피. 현재 단일 run 상태에서 6개 테이블 전부 `latest_view()` 건수 = raw 전체 건수 일치 확인 + 롤백 트랜잭션으로 합성 다회차(겹치는 날짜 대체·안 겹치는 날짜 누적) 시나리오 실제 검증(커밋 없음)
- [x] `reference/loader.py` + `/api/reference/*`(bbox·zoom, 장기 캐시) — `PORTING_PACKAGE_ROOT/사전빌드_JSON`. F5(MVP DoD)에 필요한 6종(firs/tca/airways/airports/navaids/waypoints)만 구현 — sidstar/suas/acc-sectors/firko는 2단계(`05` §3) 또는 원본 소스 부재(firko: 사전빌드_JSON에 파일 자체가 없음, 조작 금지 원칙상 임의 생성하지 않고 갭으로 남김). `main.py`(reference 라우터만 우선 등록, CORS·routes·공통에러는 다음 항목) + uvicorn 기동 후 curl로 6개 엔드포인트 전부 HTTP 종단 확인: 전체건수 대조(FIR 247·항로 89,555·공항 10,030·TCA 63, 전부 baseline 일치), bbox 필터(한반도 bbox→FIR 6개로 축소 확인), icao 필터, 잘못된 bbox→400, waypoints limit 초과→422(FastAPI Query 검증), Cache-Control 헤더, 404. **미완성으로 남긴 부분**: `zoom` 파라미터는 원본 `문서/03`이 구체적 수치 임계값을 규정하지 않아(라벨 표시 임계값만 있고 데이터 씨닝 규칙은 airports "저배율은 민간/공용만"뿐) 현재는 파라미터만 받고 airways/navaids/airports/waypoints 응답에는 아직 적용하지 않음 — 프론트(F5/F9) 실제 연동 시 확정
- [x] `batch/build_odr2.py` — `processed_flight_data`에서 ODR2 집계(집계 로직: `PORTING_PACKAGE_ROOT/전처리스크립트/{3_agg_csv,4_build_routes2}.py` 이식, 원본 무수정). 산출물은 advisor 소유(전처리 DB에 쓰지 않음, `app/reference/artifacts/odr2.json`). **초강력 검증**: `사전빌드_JSON/odr2.json`(원본 CSV 파이프라인이 만든 실제 기준선, 2026-01 데이터)과 필드 단위 diff — OD 1,487·경로 그룹 3,083 둘 다 정확히 일치 + 문서화된 9필드(n/avgMin/delayCnt/heavyCnt/firs/pixes/track/frc/parity) 전부 3,083개 그룹에서 완전 일치(불일치 0건). 과정에서 비결정성 버그 발견·수정: DB fetch에 `ORDER BY`가 없어 편수 동률 그룹의 대표/순위가 실행마다 달라질 수 있었음 → `id` 순 정렬로 원본 CSV 파일 순서 재현(수정 전 3건 불일치 → 수정 후 0건). 참고: 원본 `사전빌드_JSON/odr2.json`에는 상층풍/ACDM 유래로 보이는 추가 필드(9번째 이후, OD별 3번째 원소)가 있으나 04-E(상층풍) 등 2단계 소관이라 이번 포팅 범위에서 제외(문서화된 9필드/2원소 스키마만 재현) — ⚠ **저장소 2026-07-23 갱신**: `odr2.json` 파일 아티팩트는 폐지되고 `advisor_odr2_*` DB 테이블로 대체됨(아래 "파일 아티팩트 → DB 통합" 절 참고). 집계 로직·API 계약은 무변경.
- [x] `/api/routes*`(MVP). `/api/airports/{icao}/ops`·`/api/fois/delays`·`/api/flow-management`은 **2단계로 이관**([05](./05-mvp-scope.md) §3, [03](./03-backend-api.md) §4.2~4.4) — Stage 1 산출물 아님. `queries/routes.py`(odr2.json 아티팩트 로드+캐시, 키 기반 재구성) + `routers/routes.py`(`/api/routes/od-pairs`, `/api/routes?dep=&arr=`, ICAO 4자리 정규식 검증). uvicorn 종단 확인: od-pairs 1,487건·편수 내림차순, VHHH→RKSI 4옵션 정상 반환(track_coords가 [[lat,lon],...] 쌍으로 정상 변환), 잘못된 ICAO→422(FastAPI Query 길이 제약), 없는 OD→404
- [x] 공통 응답 봉투·에러·CORS·관측성 — `app/envelope.py`({data,meta}, docs/03 §2), `app/middleware.py`(레이트리밋: 프로세스 메모리 슬라이딩윈도우, 단일 인스턴스 전제 — 수평확장 시 공유스토어로 교체 필요 명시; 요청 로깅), `main.py`에 CORS(`CORS_ALLOWED_ORIGINS` 화이트리스트, GET만 허용)+전역 예외 핸들러(스택/내부 구현 비노출, 서버 로그에만 상세 기록). 종단 확인: 허용 오리진만 `Access-Control-Allow-Origin` 응답, `RATE_LIMIT_PER_MINUTE=3`로 4번째 요청부터 429 확인
- [x] 규모 대조(FIR 247·항로 89,555·픽스 58,812·공항 10,030·OD 1,487) — 전부 실측 일치(reference/loader.py, batch/build_odr2.py 검증 시 확인)
- [x] 공통 게이트 통과 — `senior-code-reviewer`가 이번 라운드 전체 산출물(db 레이어·latest_run·reference loader/router·batch/build_odr2·routes·envelope·middleware·main·신규 alembic 마이그레이션) 리뷰. 지적사항 5건 전부 수정 확인: ① `load_airways`의 seq가 bbox 필터 후에 매겨져 요청마다 값이 달라짐 → 필터 전 전체 데이터에서 한 번만 계산하도록 수정(필터 전/후 seq 일치 재확인) ② `latest_run.py` 날짜별 승자 선정에 `finished_at` 동률 시 타이브레이커 없음 → `run_id` 2차 정렬 추가 ③ bbox `minLon>maxLon`(날짜변경선 역전 범위)이 조용히 빈 배열을 반환 → 명확한 400 에러로 수정(단, firs 데이터 자체의 ±360 연속좌표는 그대로 허용) ④ reference 라우터가 자산 손상/누락 시 처리되지 않은 500 → routes.py와 동일하게 503으로 통일 ⑤ `build_odr2.py`의 `if tm:`이 TEET=0을 falsy로 오판해 집계 누락 → `is not None`으로 수정. 수정 후 ODR2 기준선 재검증(불일치 여전히 0건) + uvicorn 종단 재확인(전부 정상) 완료. `include_columns`/`resolve_fks=False` 리플렉션 방어, `latest_run.py`의 3단계 윈도우 로직, 미들웨어 동시성은 리뷰에서 문제없음으로 확인됨(리포트 참고)

- [x] **(2026-07-22 3차 리뷰 — 신규 세션 독립 리뷰, 2건 수정)** 이전 리뷰(항목 위 5건)에서 안 걸린 이슈 2건을 새로 발견·수정. ① **에러 응답 포맷이 [03](./03-backend-api.md) §7 "표준 {error:{code,message}}" 계약과 불일치** — 실제로는 라우터 `HTTPException(detail=...)` 전부가 FastAPI 기본값 `{"detail": ...}`로, `main.py` 500 핸들러와 `middleware.py` 429가 각각 `{"error": "<string>"}`로 나가 세 가지 서로 다른 모양이 섞여 있었음. `app/envelope.py`에 `error_envelope(status_code, message)` 추가 + `main.py`에 `StarletteHTTPException`/`RequestValidationError`/`Exception` 3종 전역 핸들러를 새로 등록해 전부 이 함수를 거치도록 통일(라우터 코드는 무수정 — 여전히 `HTTPException(detail=...)`만 던지면 됨). ② **`/api/routes*`(ODR2) 응답의 `data_period`가 항상 null** — §2가 "참조 데이터만 null" 예외를 뒀는데 DB 유래 데이터인 ODR2도 항상 null이었음(배치 산출물 `odr2.json`에 자신이 집계한 날짜 범위를 기록하지 않아 애초에 채울 수 없었음). `batch/build_odr2.py`에 `_data_period()` 추가해 집계에 쓴 실제 날짜 범위를 계산하고, 핵심 아티팩트(`odr2.json`, 원본 파이프라인과 필드 단위로 완전 일치해야 하는 산출물)는 그대로 두고 별도 사이드카 `odr2_meta.json`에만 기록 → `queries/routes.py.data_period()`가 읽어 `routers/routes.py`의 두 엔드포인트 모두 `envelope(..., data_period=...)`로 전달. `run_id`는 "일자별 최신 run 우선" 특성상 날짜별 승자 run이 다를 수 있어 여전히 null(§2에 명시). 수정 후: odr2.json 재생성 결과가 수정 전과 바이트 단위로 동일함을 diff로 확인(집계 로직 자체는 안 건드림, date 컬럼만 추가로 SELECT), uvicorn 종단 확인 — 400(ICAO 형식)·404(OD 없음)·422(FastAPI Query 검증)·429(레이트리밋, 실제로 60회 버스트해 61번째부터 429 재현)·503(아티팩트 임시 제거) 전부 `{error:{code,message}}` 단일 포맷 확인 + `/api/routes`·`/api/routes/od-pairs`는 `data_period="20260101-20260131"`, `/api/reference/*`는 여전히 `data_period=null` 확인.

**Stage 1 완료** — 위 항목 전부 체크+ 공통 게이트 통과.

## Stage 2 — 프론트 (Leaflet, fetch)

> 근거: [04](./04-frontend-migration.md). 원본 기능/알고리즘·기상서버·완성본은 `PORTING_PACKAGE_ROOT`(env).

- [x] F1 모듈 분리 + `js/config.js`(설정 주도) — `frontend/index.html`+`css/style.css`+`js/config.js`. `DEFAULT_CONFIG`(URL·줌 임계·표시상한·디자인 토큰 전부 여기 하나) + `frontend/config.json`(git 미추적, `.env`와 동일 패턴) fetch 오버레이. 루트 `config.example.json`을 실제 `DEFAULT_CONFIG`와 동기화(단일 진실원)
- [x] F2 `js/api.js`(baseURL·in-flight 중복요청 합침·`ApiError`) — F3 `js/adapters.js` — F4 `js/store.js`(구독형 상태, 파생 상수 `firByIcao`/`airportByIcao`). **F3 결정 로그**: 원본 렌더 소스 자체가 저장소에 없어(15MB, 열람 금지·정답지 전용) "기존 코드 재사용" 전제가 성립하지 않으므로 04 §4.1 대안 중 키 기반 신규 작성을 택함(docs/04-frontend-migration.md §4.1에 동기화). ODR2만 `toOptArray()`로 08 배열 형태도 병행 제공
- [x] F5 참조 레이어 6종(FIR/TCA/항공로/픽스/공항/항행시설) fetch 렌더, **결정 포커스 기본 + 전세계 온디맨드**([04 §3.1](./04-frontend-migration.md), [10 §2](./10-ui-and-realtime.md)) — `js/layers/reference.js`. 04-F(겹침 폴리곤 채움) 이식: Leaflet Canvas 기본 `fillRule:'evenodd'`가 겹치는 FIR 멀티폴리곤을 상쇄시키는 것을 실측 확인(`firs.json` 247개 중 SBAO/SBBS/SBCW/UEEE/UWWW 등 mixed-winding 실재 확인) → `fillRule:'nonzero'`+멀티폴리곤 단일 L.polygon 패스+shoelace 링 방향 통일로 수정. 공항 저배율(민간/공용만) 임계는 문서가 구체값을 안 줘 Stage1이 남긴 갭([03 §3](./03-backend-api.md) 각주 갱신)을 여기서 `airportFullTypeZoom=5`로 확정
- [x] F6 ROUTE 패널(`js/route-panel.js`+`js/layers/route.js`, `/api/routes`) — 출발(편수순)→도착 select, 브리핑, 옵션 목록(편수%/평균소요Δ/지연/HEAVY/경유FIR), 전체 겹쳐보기/개별 강조+FIR 면(오렌지 10%, nonzero)+DEP/ARR 배지
- [x] F7 공항 기상(`js/weather.js`) — 기존 Node 프록시 폴백 체인(직접→로컬→공개 프록시, 성공 경로 기억) 유지, `문서/07_기상MCP서버.md` §3 해독·위험판정 규칙 그대로 이식(`kst`/`decodeMetar`/`riskOf`), TAF 타임라인(BECMG/FM만 기저 갱신, TEMPO/PROB 오버레이) — AWC 실제 TAF/ADS-B JSON 스키마는 WebFetch로 필드명 확인 후 구현(허위 생성 금지 원칙)
- [x] F8 실시간 ADS-B(`js/layers/adsb.js`) — adsb.lol/adsb.fi/airplanes.live 폴백 폴링(12초, 반경 250NM), 헤딩 방향 회전 기체 아이콘 + 편명/노선 상시 2줄 라벨(배치 캐시, 아래 2026-07-23 항목 참고), 클릭 시 상세(고도/속도/정확한 노선) 조회. `문서/05_트러블슈팅.md` "3연속 실패 시 30초 백오프·마지막 데이터 유지" 규칙 이식
- [x] F9 뷰모드 토글 3종(`js/viewmode.js`), F10 미니맵(`js/minimap.js`, [10 §2.1·§2.3](./10-ui-and-realtime.md)) — "지역 컨텍스트"는 FIR 인접 그래프 계산 없이 고정 bbox(`config.map.regionContextBounds`)로 근사(2단계 buildGeo급 인접판정은 MVP 범위 밖)
- [x] 완성본 HTML 정답지와 **전세계 모드** 회귀 비교(직접 Read 금지, [06 §6·§7](./06-conventions.md)) — 실제 로컬 백엔드(uvicorn)를 기동해 Node 스텁 하네스(DOM 없이 config→api→adapters→store 전체 흐름 + 최소 Leaflet 스텁으로 레이어 렌더 흐름)로 건수 대조: FIR 247·항공로 89,555·공항 10,030·픽스 800(상한, 원본도 동일 상한)·TCA 63·항행시설 8,056·OD 1,487·VHHH→RKSI 옵션 4건 전부 Stage1 기준선과 완전 일치. `node --check` 전체 통과
- [x] **미구현/축소 스코프(투명하게 남김)**: 기상 레이더(RainViewer)·SIGMET/PIREP 토글([10 §2.5](./10-ui-and-realtime.md) 레이어⑤·④, F1~F10에 대응 작업 없음 — 문서 간 스코프 불일치 발견, 향후 별도 작업으로 추가 필요)·SID/STAR·SUAS·ACC 관제량·상층풍/시어 오버레이·FIR 분석 패널·비행 애니메이션은 2·3단계 소관. 공개 CORS 프록시는 원본 4종 대신 검증 가능한 2종(corsproxy.io/allorigins.win)만 채택(허위 URL 생성 금지 원칙상 미검증 URL 추가 안 함, `config.json`으로 로컬에서 추가 가능)
- [x] 공통 게이트 통과 — `senior-code-reviewer`가 `frontend/` 전체(config/api/adapters/store/net/weather/route-panel/viewmode/minimap/main.js + layers/{reference,route,adsb}.js) 리뷰. 지적사항 14건 중 실코드 결함 9건 수정 완료(나머지 5건은 문서화된 의도적 축소/스코프 불일치 기록으로 처리):
  1. **[Critical] ADS-B 상시 라벨 XSS** — 콜사인이 `permanent:true` 툴팁에 이스케이프 없이 렌더돼 클릭 없이도 지도를 보는 모든 사용자에게 실행 가능 → `escapeHtml()`(`js/html.js` 신규) 적용
  2. **[Critical/High] ADS-B 클릭 상세 팝업 XSS** — 콜사인/기종/등록기호/adsbdb 노선 코드 미이스케이프 → 동일 수정
  3. **[High] METAR/TAF 원문·wxString XSS** — `weather.js`의 `innerHTML` 삽입 지점 다수 미이스케이프 → `decodeMetar`/`riskOf`/`buildTafTimeline`은 순수 데이터 함수로 유지하고 렌더 경계(`renderAirportWeatherInto`/`tafHtml`)에서만 이스케이프
  4. **[Medium] 참조 데이터(FIR/TCA/항공로/픽스/항행/공항) popup·tooltip·divIcon 미이스케이프** — `data-weather-icao` 속성 컨텍스트 이탈 가능성 포함 → 전부 이스케이프(정적 사전빌드 데이터라 실위험은 낮지만 방어적 코딩 일관성)
  5. **[Low] FIR 배지 미이스케이프** — `main.js`도 동일 수정
  6. **[High] 뷰모드 전환 실패 시 `state.viewMode`가 먼저 바뀌어 렌더 분기가 아예 안 타는 논리 버그** — 벌크 로드 성공 후에만 `state.viewMode` 반영하도록 수정, 합성 실패 시나리오로 재현·수정 확인
  7. **[Medium] `selectOd` 경쟁 조건**(빠른 연속 선택 시 느린 응답이 최신 선택을 덮어씀) — 요청 세대(generation) 토큰 가드 추가, 인위 지연으로 재현·수정 확인
  8. **[High] API 에러 파싱 버그** — 백엔드 표준 에러 봉투 `{error:{code,message}}`(이미 구현돼 있었음, `app/envelope.py:error_envelope`)를 프론트가 `body.error`를 문자열로 오인해 `"[object Object]"`로 표시하던 버그 → `body.error?.message` 우선 참조로 수정, 실제 400 응답으로 재현·수정 확인
  9. **[Medium] ADS-B 폴링에 실패 백오프 없음** — `문서/05_트러블슈팅.md` 규칙(3연속 실패 시 30초 백오프, 마지막 데이터 유지)대로 수정
  - 문서화 처리(코드 수정 없음): 레이더/SIGMET·PIREP 스코프 갭, 라벨 줌 임계 일부 미사용(→ 오히려 이번에 TCA/픽스/항행/공항ICAO 영구 라벨을 전부 줌게이팅하도록 보강해 해소), `REGION_CONTEXT_BBOX`/웨이포인트 `#fff` 하드코딩(→ `config.js`/`CONFIG.tokens.paper`로 이동해 해소), 공개 프록시 2종 축소(위 스코프 갭 항목에 기록)
  - 수정 후 재검증: 기존 회귀 스모크(건수 대조) 전부 재통과 + 신규 스모크(에러 파싱·escapeHtml 적용·뷰모드 실패 상태보존·selectOd 경쟁조건) 추가 통과

**Stage 2 완료** — 위 항목 전부 체크 + 공통 게이트 통과.

---

## 완료 — result/ 검증

- [x] `flight-route-advisor/result/`에 초기 문서 대비 구현 대조 리뷰 작성([result/review-2026-07-22.md](../result/review-2026-07-22.md)) — docs 02·03·04·05 문서대조표(§H) + Stage0~2 통합관점 리뷰(§A~C) + 우선순위 수정 큐(§0)
- [x] 리뷰에이전트 최종 통과(기능·논리·예외·보안) — cross-stage 신규 발견 13건(Stage0 rate limit/워크스페이스 누수/고착run/--reload, Stage1 500→503/hits누수/bbox nan/regex deprecated, Stage2 뷰모드경쟁/FIR윈딩/ADS-B정리/api.js 캐시회귀) **전부 수정·재검증 완료**(uvicorn 실기동+curl, synthetic DB 트랜잭션, Node 스모크 하네스 5종 재통과). 완전 선택 항목 2건(모듈캐시 스레드경합-양성, 프록시경로 세션간 미persist)만 잔존
- [x] **불일치 시 사용자에게 설명·처리 질문**(임의 수정 금지) — [result/review-2026-07-22.md §D](../result/review-2026-07-22.md) 6건 전부 사용자 확정 후 처리 완료(rate limit·고착run 코드보완, 레이더/SIGMET-PIREP 2단계 이관 문서화, 프론트 동일 오리진 통합, docs/02 §8 표기 정리, README.md 실제 완료 상태로 갱신). 갱신 중 루트 `start.sh`가 동일 오리진 결정과 어긋나게 프론트를 별도 포트(5173)로 이중 서빙하던 잔여 코드를 발견해 함께 정리(`start.sh`·`.env.example`의 `FRONTEND_PORT`/`FRONTEND_HOST`/`CORS_ALLOWED_ORIGINS` 예시값)

## 2단계 — FOIS 지연원인 패널 (착수, 2026-07-22)

> 근거: [03 §4.3](./03-backend-api.md), [05 §3](./05-mvp-scope.md). 2단계 항목 중 난이도·선행조건이 가장 낮아 최우선 착수(사용자 확정).

- [x] `backend/app/queries/fois.py` — `latest_run.latest_view()` 기반 조회, `direction`(dep\|arr)별 `processed_fois_departure`/`processed_fois_arrival` 선택, `cause_major/cause_minor/cause_process/involved_party/reason` group by + count, `airport`/`date_from`/`date_to` 필터. `data_period`는 필터링된 윈도우의 실제 날짜 min/max(결과 0건이면 null), `run_id`는 ODR2(§4.1)와 동일한 이유로 항상 null.
- [x] `backend/app/routers/fois.py`(`GET /api/fois/delays`) — `direction` 필수(Query pattern `^(dep|arr)$`), `airport` ICAO 형식 검증(routers/routes.py의 `_validate_icao`와 동일 스타일), `date_from`/`date_to` `YYYY-MM-DD` 정규식 검증 + `date_from>date_to` 400, DB 연결 실패(`OperationalError`/`DBAPIError`) 503. `main.py`에 라우터 등록.
- [x] 프론트 `js/fois-panel.js` + `js/api.js:foisDelays` — ROUTE 패널(F6)과 달리 뷰모드/OD 선택과 무관한 독립 조회 도구라 `store.js` 전역 상태에 얹지 않고 모듈 내부 세대(seq) 토큰으로 경쟁 조건만 방지(selectOd와 동일 패턴). `index.html` `panel-right`에 마크업 추가(구분 select·공항 input·기간 date range·조회 버튼·결과 목록), `style.css`에 `.fois-causes` 등 스타일 추가. 결과 렌더는 `textContent`만 사용(HTML 삽입 없음 — escapeHtml 불필요, js/layers/*의 innerHTML 경로와 다름).
- [x] uvicorn 실제 기동 + curl로 필터·에러 케이스 종단 확인: direction 누락/오타 422, airport 형식오류(4자리 비ICAO) 400, airport 길이오류 422(routers/routes.py의 ICAO 검증과 동일하게 Query min/max_length가 먼저 걸림), date 형식오류 400, date_from>date_to 400, 정상 조회(direction=arr&airport=RKSI&기간필터) 응답 확인 — `airport=null,total=8893`(무필터)·`RKSI 2026-01`(4,481건·144종 원인조합), `meta.data_period`가 필터링된 실제 날짜 범위로 정확히 채워짐, `run_id`는 의도대로 null
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — `senior-code-reviewer`가 8개 변경 파일 전부(백엔드 3·프론트 5) + 의존 파일(latest_run/column_map/tables/session/envelope/routes.py/adapters/store.js) 리뷰. Critical/High 없음, Low 3건 전부 수정 확인: ① `frontend/js/fois-panel.js`가 다른 모든 API 소비 코드와 달리 `adapters.js`의 `toX()` 어댑터를 안 쓰고 원본 snake_case를 직접 읽던 것 → `adapters.js`에 `toFoisCause()` 추가해 통일 ② `backend/app/routers/fois.py`의 날짜 검증이 자릿수 모양만 보고 `2026-13-99` 같은 존재하지 않는 날짜를 통과시키던 것 → `date.fromisoformat()` 캘린더 검증 추가(재검증: 400 확인) ③ 조회 버튼 클릭에만 걸려있어 Enter 키 제출이 안 되던 것 → `<form>`+submit 이벤트로 전환. SQL 인젝션(바인드 파라미터만 사용)·검증 순서(라우터에서 먼저 검증 후 쿼리 호출)·XSS(textContent만 사용, escapeHtml 불필요 확인)·경쟁조건 가드(store.js의 selectOd와 동일한 compare-after-await 패턴)·빈 결과셋 처리(division-by-zero 없음, period NULL-safe)·미들웨어/CORS 우회 없음 전부 문제없음으로 확인. 성능 최적화(agg/period 쿼리 병합) 1건은 낮은 우선순위 향후 개선으로 남김.

## 2단계 — 흐름관리 조회 패널 (착수, 2026-07-23)

> 근거: [03 §4.4](./03-backend-api.md), [05 §3](./05-mvp-scope.md). FOIS(2026-07-22) 다음으로 난이도가 낮은 항목(사용자 확정).

- [x] `backend/app/queries/flow_management.py` — `latest_run.latest_view("processed_flow_management")` 기반 목록 조회(단일 테이블, FOIS처럼 dep/arr 분기 없음). `date_from`/`date_to`는 최신본 윈도우와 같은 컬럼인 `record_date`에 매칭(문서 초안의 `apply_start_dt` 필터안은 채택 안 함 — 윈도우·필터 컬럼 불일치 방지). `fir`/`airway`는 `target_fir`/`target_route` 완전일치(실측상 콤마 목록이 아닌 단일 값). `apply_start_dt` 1차 + `flow_id` 2차 정렬(동률 타이브레이커, `latest_run.py`/`build_odr2.py`에서 겪은 비결정성 함정과 동일 대비). `limit`(기본 100/최대 500)·`offset` 페이지네이션 + 필터 전체 기준 `total`/`data_period`.
- [x] `backend/app/routers/flow_management.py`(`GET /api/flow-management`) — `date_from`/`date_to` 형식+캘린더 검증(FOIS 리뷰에서 나온 `date.fromisoformat()` 패턴 처음부터 반영) + 역전 400, `fir`/`airway` 영문·숫자 1~10자 검증, `limit`/`offset` Query 제약(`ge`/`le`), DB 연결 실패 503. `main.py`에 라우터 등록.
- [x] 프론트 `js/flow-management-panel.js` + `js/api.js:flowManagement` + `js/adapters.js:toFlowManagementItem` — FOIS 패널과 동일하게 store.js 전역 상태와 무관, `<form>`+submit(Enter 키 제출), 어댑터 통일(FOIS 리뷰 지적사항을 처음부터 반영). "더 보기" 버튼으로 offset 누적 로드 + 연타 시 중복 로드 방지(버튼 disable), 조회 세대(seq) 토큰으로 새 검색 시작 시 이전 페이지 응답 폐기.
- [x] uvicorn 실제 기동 + curl로 필터·페이지네이션·에러 케이스 종단 확인: 무필터(총 378건, limit 100)·`fir=RKRR`(35건, 실제 DB `WHERE target_fir='RKRR'` 카운트와 일치)·`limit=2&offset=2`가 실제 `apply_start_dt` 정렬 순서(00:00→04:30→04:30→09:00→09:20, id 순서 아님)로 정확히 3·4번째 항목을 반환함을 수작업 대조로 확인·`fir` 길이초과 422·날짜 캘린더오류(`2026-02-30`) 400·`date_from>date_to` 400·`limit` 상한초과 422
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — `senior-code-reviewer`가 9개 파일(백엔드 2·프론트 5·의존 어댑터/FOIS 대조 2) 리뷰. **High 1건 수정**: `flow-management-panel.js`의 "더 보기" 클릭 중 새 검색을 제출하면, seq 가드는 데이터 오염은 막았지만 `moreBtn.disabled`는 오래된 클릭의 `finally`(자기 seq만 확인)가 되돌리지 못해 버튼이 영구히 비활성 상태로 남는 버그 발견 → `search()` 시작 시 `moreBtn.disabled=false`를 명시적으로 리셋하도록 수정, Node로 정확한 seq/disabled 상호작용을 시뮬레이션해 수정 전 재현·수정 후 해소 확인. 그 외(SQL/페이지네이션 정확성, 빈 결과셋 처리, 시큐어코딩, fois.py/fois-panel.js 컨벤션 일치) 전부 문제없음으로 확인.

## 2단계 — 노선-FOIS 라이트 연동 (착수, 2026-07-23)

> 배경: 사용자가 FOIS 지연원인 패널 스크린샷을 보고 "비행경로 추천과 연관지어 쓸 수 있는가" 질문 → 검토 결과 FOIS는 노선(OD) 단위가 아니라 공항 단위 집계라 "그래서 이 경로를 추천한다"는 인과관계로 쓰면 과장 위험이 있음을 확인. **1단계(가벼운 자동연동)만 우선 적용해 실사용 양상을 지켜보고, 2단계(추천 로직 반영)는 계획만 세워둔다**(사용자 확정, 2026-07-23). 2단계 계획은 아래 "향후 확장" 절 참고.

- [x] `frontend/js/route-fois-summary.js`(신규) — `store.js`의 `od:selecting`/`od:selected`/`od:error` 이벤트만 구독하는 독립 위젯(FOIS 지연원인 패널 자체는 손대지 않음). 노선 선택 시 출발/도착 공항의 `/api/fois/delays`(direction=dep/arr)를 `Promise.allSettled`로 동시 조회해 상위 3개 원인만 "의사결정 근거" 패널(`#route-fois-summary`, `fir-chain` 바로 아래)에 텍스트로 표시. 한쪽 조회 실패가 다른 쪽을 막지 않음(`allSettled`), 결과 0건은 "최근 지연원인 기록 없음"으로 구분 표시. 조회 세대(seq) 토큰으로 노선을 빠르게 연속 전환해도 오래된 응답이 최신 표시를 덮어쓰지 않음(선택 시작 시 `clear()`가 seq를 증분해 이전 요청 무효화 — FOIS/흐름관리 패널과 동일 계열 가드).
- [x] `index.html`/`style.css`/`main.js` 연결 — `#route-fois-summary` 마크업, `.route-fois-summary` 스타일, `initRouteFoisSummary()` 부트 결선.
- [x] uvicorn+curl로 실제 OD 쌍(RKPC→RKSS)의 dep/arr FOIS 응답 확인(RKPC 1702건, RKSS 1108건, 상위원인 정상 파싱) + 데이터 없는 공항(ZZZZ) 조회 시 `total=0`로 정상 처리되는 경로 확인.
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — `senior-code-reviewer`가 seq 가드(flow-management의 `moreBtn.disabled` 부류 버그 없음 확인)·`Promise.allSettled` 인터리빙·`od:error` 시 잔여 텍스트 없음·어댑터/XSS/신규 백엔드 표면 없음을 전부 확인. 발견된 이슈 없음(Go).

## 2단계 — 공항 운항 KPI(ACDM) 패널 (착수, 2026-07-23)

> 근거: [03 §4.2](./03-backend-api.md), [05](./05-mvp-scope.md) §3. 사용자 요청으로 2단계 잔여 중 착수(경로 기하 동적화·상층풍/시어·참조 타일화·기상 레이더/SIGMET·PIREP과 함께 지시됐고, 그중 가장 난이도·선행조건이 낮아 먼저 구현).

- [x] `backend/app/queries/airport_ops.py`(신규) — `latest_run.latest_view()`로 `processed_acdm_departure`/`processed_acdm_arrival` 조회, `airport_icao`+선택적 `date_from`/`date_to` 필터. **실측으로 발견한 함정**: 이 KPI 컬럼들(`departure_punctuality_min` 등 5종)이 DB에 **text**로 저장돼 있어(스킬의 `number_text()`가 NaN을 NULL이 아닌 빈 문자열로 직렬화) `text <= integer` 비교가 Postgres 에러(`operator does not exist`)로 죽는 것을 실측 확인 → `_numeric()` 헬퍼(`nullif(col,'')::numeric`)로 해결. `ctot_slot_adherence`는 숫자가 아니라 범주 문자열("조기"/"준수"/"지연"만 판정 모집단, "CTOT없음"/"계산불가" 제외)이라는 것도 원본 스킬 코드(`prepare_acdm_departure_core5.py`)를 직접 읽어 확인 후 반영. **`on_time_rate` 15분 임계값은 원본 어디에도 없어(부호·단위만 정의) EUROCONTROL/ICAO 산업표준 On-Time Performance 정의를 이번에 확정**(코드/문서에 근거 남김, [03 §4.2](./03-backend-api.md) 참고 — 도메인 판정값이라 재검토 여지 있음).
- [x] `backend/app/routers/airports.py`(신규, `GET /api/airports/{icao}/ops`) — ICAO 4자리(Path+정규식 이중 검증)·날짜(`date.fromisoformat()` 캘린더 검증, FOIS 리뷰에서 나온 패턴 그대로 반영)·역전 400, DB 연결 실패 503. `main.py`에 라우터 등록.
- [x] 프론트 `js/ops-panel.js` + `js/api.js:airportOps` + `js/adapters.js:toAirportOpsSummary` — FOIS/흐름관리 패널과 동일하게 store.js 전역 상태와 무관, `<form>`+submit(Enter 키 제출), 조회 세대(seq) 토큰. 결과는 `textContent`만 사용(HTML 삽입 없음).
- [x] uvicorn+curl 종단 확인 + **독립 psql 쿼리로 응답값 전부 직접 대조**(RKSI 출발 19,606편·정시율 0.7805·CTOT준수 1207/1277=0.9452, 도착 19,580편 등 소수점까지 일치) + 데이터 없는 공항(ZZZZ→전부 null, 에러 아님)·기간필터·에러케이스(icao 길이 422, 날짜역전 400, 존재하지 않는 날짜 400) 확인. 프론트는 Node에 최소 DOM 스텁을 씌워 실제 백엔드에 붙여 정상조회·클라이언트 검증·빈 데이터·경쟁조건(세대 토큰) 4가지 시나리오 테스트 통과.
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — `senior-code-reviewer`가 신규 3파일 + 5개 파일 diff(`main.py`/`api.js`/`adapters.js`/`style.css`/`index.html`) 리뷰, 원본 스킬 소스까지 직접 대조해 text 캐스팅·CTOT 분모/분자·15분 임계값 채택 타당성 검증. Critical/High 없음. Low 2건: ①이 문서(§4.2)가 원래 원시 컬럼(`ttot/ctot/eldt/aldt`)을 쓸 것처럼 적혀 있던 문서 드리프트 → 구현(파생 KPI 컬럼만 사용)에 맞게 갱신 완료, ②`_validate_date`에 `.strip()` 누락은 `fois.py`와 동일한 기존 관례라 이번 diff가 만든 새 문제 아님(미수정, 향후 일괄 정리 대상). **리뷰어 권고**: 15분 임계값 채택은 타당한 산업표준 적용이나 도메인 판정을 바꾸는 결정이라 사용자 재확인 권장 — 사용자 확정(2026-07-23): 15분 기준 그대로 채택.

## 2단계 — 기상 레이더(RainViewer) + 위험기상(SIGMET/PIREP) 토글 (착수, 2026-07-23)

> 근거: [10 §2.5](./10-ui-and-realtime.md) 레이어④·⑤. 사용자 요청으로 ACDM KPI 다음 착수. 프론트 전용(백엔드 변경 없음) — AWC(SIGMET/PIREP)는 기존 METAR/TAF와 동일한 CORS 프록시 폴백 체인 재사용, RainViewer는 CORS `*` 허용이라 직접 호출.

- [x] `frontend/js/layers/radar.js`(신규) — `api.rainviewer.com/public/weather-maps.json` 온디맨드 조회(레이어 토글 on 시), `radar.past`(과거 2시간 프레임)를 타일레이어로 로드해 재생바(`#radar-bar`)로 프레임 전환. `maxNativeZoom:6`(원본 문서/05 트러블슈팅 "Zoom Level Not Supported" 회피).
- [x] `frontend/js/layers/hazards.js`(신규) — AWC `isigmet`/`pirep` 조회해 SIGMET은 폴리곤(`geom`이 AREA/AREAS 두 형태인 실제 응답을 실측 확인해 통일 처리, 퇴화 링 필터링), PIREP은 원형 마커로 토글 레이어에 렌더. 팝업 전부 `escapeHtml` 경유.
- [x] `frontend/js/weather.js` — `awc()`의 path→URL 매핑을 4종(metar/taf/isigmet/pirep)으로 확장, `getSigmets()`/`getPireps()` 추가(기존 폴백 체인 그대로 재사용). `config.js`/`config.example.json`에 `weather.sigmetUrl/pirepUrl`, `radar.*`(URL·타일 파라미터·재생 간격) 추가.
- [x] `main.js` — 레이어 컨트롤에 "SIGMET(위험기상)"·"PIREP"·"기상 레이더" 3항목 추가(전부 기본 OFF), `overlayadd`에서 온디맨드 조회(SIGMET 전세계 1회, PIREP은 현재 지도 뷰포트 bbox).
- [x] **의도적으로 축소한 범위**: [10 §2.5](./10-ui-and-realtime.md) ④ 원안의 "루트 교차 시 지도 경고 마커 + 근거 패널 칩(TS·ICE·TURB)" 중 경로-폴리곤 교차판정 로직은 이번에 미구현 — 원본 어디에도 판정 알고리즘 근거가 없어 발명하지 않음(허위 정보 생성 금지 원칙). 이번엔 토글 레이어로 지도에 SIGMET/PIREP을 표시하는 것까지만 구현, 교차판정은 사용자 확인 후 별도 착수 여부 결정.
- [x] 실측 검증: 실제 AWC/RainViewer API에 curl로 직접 붙어 스키마 확인(SIGMET `coords`가 geom값에 따라 다른 모양, PIREP 필드, RainViewer 프레임/타일 URL) — 허위 필드 생성 없이 실제 응답 기준으로 구현. Node에 Leaflet/DOM 최소 스텁을 씌워 실제 API에 붙여 SIGMET 111건 조회·110건 렌더(1건 좌표 없음 정상 드롭)·AREAS 멀티링 정상 파싱·PIREP 78건 렌더·escapeHtml로 XSS 실제 재현 후 무력화 확인·RainViewer 11프레임 로드+단일 프레임 표시 확인.
- [x] 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트) 통과 — `senior-code-reviewer` 리뷰에서 Critical/High(보안) 없음, High 1건(예외처리) + Medium 2건 발견·전부 수정: ①SIGMET/PIREP 조회 실패 시 조용히 빈 레이어로만 남아 "위험기상 없음"과 "조회 실패"를 구분 못하던 것 → `toast()` 호출 추가(기존 boot/od 실패와 동일 컨벤션 통일) ②`radar.js`의 `loaded` 플래그가 최초 로드 후 리셋되지 않아 토글을 껐다 켜도 오래된 프레임에 계속 머물던 버그 → `overlayremove`에서 리셋해 재토글 시 항상 재조회하도록 수정(재수정 후 Node 스모크로 재검증: 재토글 시 실제로 재fetch됨 확인) ③레이더 프레임 조회 실패 시 재생바가 무반응 상태로만 남던 것 → "레이더 로드 실패" 라벨 표시로 수정. Low 1건(SIGMET/PIREP `overlayadd` 핸들러에 seq 가드 없음)도 함께 수정(selectOd/setViewMode와 동일한 세대 토큰 패턴 적용).

## 향후 확장 (범위 밖 — 착수 시 별도 체크리스트)
- [ ] 2단계 잔여: 경로 기하 동적(04-D)·상층풍/시어(04-E)·참조 타일화·SIGMET/PIREP 루트 교차 판정(위 "의도적으로 축소한 범위" 참고)
- [ ] 3단계: 실시간 STCA/CPA·흐름관리 탭(**통합데이터·영향상세 테이블 선행**, 비행편 영향 결합)·FIR 분석 패널

### 계획(확정 v1.0, 착수 대기) — AI 경로추천 근거화 (docs 11~14)

> ✅ **결정 확정**: [14-improvement-request](./14-improvement-request.md) v1.0(2026-07-23) — 방향 A(**완성본 결정론 로직 이식 우선 + 그 위 얇은 LLM 서술층**) + 실시간 ADS-B. 구현 스펙 = [13-ai-reasoning-dev-plan](./13-ai-reasoning-dev-plan.md), 배경 분석 = [11](./11-ai-route-reasoning-proposal.md)/[12](./12-operational-goal-and-scenarios.md).
> ⚠ **착수 게이트**([13 §0](./13-ai-reasoning-dev-plan.md)): 아래 STEP은 **현재 빌드(위 "향후 확장"의 2단계 잔여 + 필요한 3단계분) 완료 후에만** 착수한다. 그 전까지 전부 미착수 유지. 각 STEP은 공통 게이트(하드코딩·시큐어코딩·리뷰에이전트)를 그대로 적용하고, 완료 시 관련 문서([13 §10](./13-ai-reasoning-dev-plan.md)) 동기화. **STEP 순서·수용기준·보안 규약은 [13](./13-ai-reasoning-dev-plan.md)이 단일 출처**(여기 목록은 진행 추적용).
> 이전 "FOIS 원인 기반 근거화" 검토 메모는 이 계획에 **흡수**됨 — 당시 미확정 4건은 doc 14에서 해소: FOIS는 공항 단위 유지·`reason` 원문 제외(D7)·순위는 결정론이 정하고 AI는 근거 서술만(D2)·"참고 정보" 톤 유지.

**Phase A — 결정론 신호 이식 (AI 아님)**
- [x] A1 흐름관리 OD·경로별 영향률 서빙 + 노선 위젯 (`batch/build_flow.py`→`flow.json`→`queries/flow_reasoning.py`→`GET /api/routes/flow`→`route-flow-summary.js`, 2026-07-23 완료) — ⚠ **저장소 2026-07-23 갱신**: `flow.json` 파일 아티팩트는 폐지되고 `advisor_flow_*` DB 테이블로 대체됨(아래 "파일 아티팩트 → DB 통합" 절 참고). API 계약·프론트는 무변경.
  - **판정 로직 출처**: 완성본 `flow.json`을 만든 계산 스크립트는 이식 패키지 어디에도 없었다(재확인: `nAff`/`ponA` 등 필드명으로 `project/result/` 전체 검색해도 완성본 HTML과 `5_build_map3.py`만 나오고 둘 다 이미 만들어진 flow.json을 읽기만 함). 대신 `SOURCE_PROJECT_ROOT/skills/preprocess-flow-management/scripts/run_flow_management_preprocessing.py`의 `integrate_flights()`(Stage 0에 이미 이식된 흐름관리 전처리 스킬 안에 있던, "항공편이 흐름관리 조치에 영향받았는가" 판정 로직)를 찾아 그대로 포팅(원본 무수정) — 대상공항/FIR/항공로/지점 다차원 교집합 + 진입/이탈 지점 통과시각 우선순위 매칭. 완성본 flow.json의 알려진 OD 3건(VHHH|RKSI·RKSI|ZMCK·RKSI|VVNB)에서 `nAff`/`nTot`/`pct`가 **정확히 일치**함을 실측 검증(`lim`/`mid`도 대부분 바이트 단위 일치, `hrP`는 24시간대 중 22개 일치·2개만 "-1(무기록)" vs "0(0%)" 표기 차이).
  - **미구현(정직하게 명시)**: `ponA`/`ponN`(영향군·평시 정시율)·`dlyA`/`dlyN`(평균지연)은 `null`. ACDM 도착 정시성(`processed_acdm_arrival`, callsign=flight_icao 조인)으로 여러 방식(임계값 0/15분, 지연양수만 평균 등) 시도했으나 완성본 수치와 1.5~2배 차이가 나 검증된 계산식을 확정하지 못함 — 사용자 확인 후 근사치를 내보내는 대신 null로 확정(2026-07-23). `rs`(주 사유)도 완성본의 한글 라벨("교통량 조절" 등) 대신 원시 `reason_code`(F/C·M/A·WX·미기재 등)를 그대로 노출 — 코드→한글 디코딩표가 어디에도 없어 임의 번역하지 않음.
  - `senior-code-reviewer` 리뷰 통과 — High 1건 수정: `_split_values()`/`entry_fir`/`exit_fir`가 원본 `clean()`(공백정규화+대문자화)을 안 거쳐, 대상 필드에 소문자가 섞이면 조용히 매칭이 빠지는 잠재 버그(현재 데이터엔 그런 값이 없어 활성 버그는 아니었으나 향후 흐름관리일지 재입수 시 발현 가능) → `_clean()` 추가로 수정, 재검증(3개 OD 동일하게 정확 일치, 회귀 없음). Low 1건(프론트 "준비 중" 문구가 `on_time_affected` 값을 직접 참조하지 않고 고정 텍스트)은 현재 필드가 항상 null이라 영향 없어 보류.
  - uvicorn+curl 종단 확인: 3개 OD 전부 정확한 수치, 기록없는 OD(`ZZZZ|YYYY`)는 404 아닌 `found:false` 200 확인(수용기준), 잘못된 ICAO 422(기존 `/api/routes`와 동일 컨벤션), 기존 `/api/routes`·`/api/routes/od-pairs`·`/api/reference/firs` 회귀 없음 확인. XSS 없음(`route-flow-summary.js`는 `textContent`만 사용).
- [x] A2 OD 시간대별 교통량/소요시간 서빙 (`build_odhr.py`→`odhr.json`→`queries/odhr.py`→`GET /api/routes/delay-history`, 2026-07-23 완료·범위축소)
  - **A1과 동일한 갭, 이번엔 대체 스킬도 없음**: 완성본 `odhr.json`의 `od[..].hr[hour]` 6원소(`n,dly,txS,txN,teS,teN`) 중 `n`(편수)·`teS`/`teN`(TEET 합/유효건수)만 실측 검증(golden VHHH|RKSI 24시간대 전부 정확 일치), `ap[icao]`(공항별 시간대 출발량)도 24시간대 히스토그램 정확 일치. `dly`(지연편수, "시간대 정시율"의 근거)는 ACDM 임계값·FOIS 당일/익일 매칭 등 여러 시도에도 golden과 불일치했고, A1 때와 달리 대체 계산 스크립트도 못 찾음(전처리 스킬 4종 전체 검색 결과 없음) — `on_time_pct`는 항상 `null`로 정직하게 반환.
  - `CAUSES`는 완성본처럼 고정 사전이 아니라 `processed_fois_{departure,arrival}.cause_major` 실측 distinct 집계로 구현(골든 13종과 정확히 일치 확인), `reason` 원문 컬럼은 읽지 않음(D7).
  - `senior-code-reviewer` 리뷰 통과 — 코드 결함 없음(Medium 1건은 문서 정합: `docs/13` STEP A2 수용기준이 여전히 "정시율 일치"로 남아있고 §7 스키마 예시 필드명이 실제 응답과 달랐던 것 → 둘 다 실제 계약(`on_time_pct: null`, `window:{hour,flights,avg_teet_min,delta_vs_baseline_min}`)에 맞게 수정).
  - uvicorn+curl 종단 확인: `hour` 미지정/지정(±1시간 윈도우, 자정 래핑 0↔23 확인)·기록없는 OD(`found:false`)·`hour=24`(422 범위검증) 전부 정상, `n`/`teS`/`teN`/`ap`/`CAUSES` golden과 정확 일치 재확인.
- [x] A3 상층풍·시어·추천고도 (client-side, 2026-07-23 완료) — `frontend/js/layers/wind.js`
  - **A1/A2와 달리 원본 알고리즘이 완성본 HTML에 그대로 존재**(`routeWind`/`segWindAt`/`calcFL`/`routeParity`/`catGrade`/`drawShear`/`renderWind`, 완성본 라인 2661~2840) — 과거 데이터 역산이 필요 없어 줄 단위로 그대로 포팅(로직 변경 없음). Open-Meteo GFS 다중 기압면(700~200hPa=FL100~390) 벡터보간·배풍/측풍 분해·시어(kt/1000ft)·추천고도(순항고도 후보 중 시어≤7 안전풀에서 소요단축 최대, 동률시 시어 낮은 쪽) 전부 이식.
  - URL·기압면 매핑·순항고도 후보(홀/짝)·TAS·시어 임계값·표본점 수는 전부 `config.js`/`config.example.json`의 `CONFIG.wind`(하드코딩 금지). 시어 색상은 `tokens.shearWeak/shearModerate/shearStrong`(orange/hazardRed와 별개 — SIGMET 색상충돌 교훈 재적용).
  - **검증 방식(실시간 외부 API라 golden JSON 대조 불가)**: 순수함수(`segWindAt`/`calcFL`/`routeParity`/`recommendFL`/`catGrade`)를 합성 입력으로 단위 검증(앵커점 보간 정확·순수 배풍/정풍 부호·180도 반전 시 큰 시어·홀짝 경계·추천 정렬 tie-break 전부 확인) + 실제 Open-Meteo API 라이브 호출로 종단 확인(홍콩→인천 근사 좌표로 6개 표본점·정상 응답·calcFL 정상 산출).
  - `senior-code-reviewer` 리뷰 통과 — Critical/High 없음. Low 3건 수정: ①FL버튼 클릭 시 `drawShear` 중복호출 제거 ②에러 메시지에 불필요한 `escapeHtml`(textContent라 이스케이프 불요, 오히려 향후 표시버그 소지) 제거 ③완성본 `#rp-wind.hw`(정풍·강한시어 시 경고배경) 대응 스타일 누락 지적 → `.route-wind-warn` 클래스로 이식.
  - `main.js`: 특정 경로 옵션이 선택됐을 때만(`selectedOptionIndex!=null`) `windLayer.update()` 호출, "전체 겹쳐보기"·선택해제 시 `windLayer.clear()`(doc13 "선택된 경로에 대해"와 일치). seq 토큰으로 경쟁조건 방지(다른 위젯과 동일 패턴) — 리뷰에서 별도 확인.
  - Playwright로 실제 브라우저 확인 완료(동시편집 세션의 일시적 상태·공유 rate limit로 몇 차례 재시도 필요했으나 최종 성공): RKSI 출발 노선 선택 → 경로 옵션 클릭 → `#route-wind` 패널 정상 노출·추천 FL/배풍/시어 정상 표시(예: "FL290 — 배풍 13kt · ▼1분 단축 · 난류 약함 이하"), FL 버튼 클릭 시 다른 고도(FL180) 수치로 정확히 갱신 확인, 콘솔 에러 0건. "전체 겹쳐보기" 전환 시 패널 숨김도 확인.
- [x] A4 실시간 ADS-B 섹터 교통·수요예측 (+10/40분 대권 외삽, 임계값 config, **순수함수 단위테스트로 완성본 동일 출력** 고정, 2026-07-24 완료) — `frontend/js/analyze-sectors.js`
  - **선행(섹터 참조 레이어 신규)**: `acc_sectors.json`(한국 인천/대구 ACC 14개 섹터만 커버하는 정적 데이터, doc13 §11 "ACC 섹터 지오메트리" 선행)이 `reference/loader.py`에 없었다 — `reference_acc_sector`/`reference_acc_boundary` 2개 테이블(마이그레이션 `b7d3f9a1c8e4`, `migrate_static_reference_to_db.py` 확장) + `GET /api/reference/acc-sectors`(docs/03 §3에 이미 명세돼 있었으나 미구현 상태였음) 신규 구현. seq 컬럼으로 원본 배열 순서 보존(아래 참고).
  - **핵심 로직 이식**: 완성본 `analyzeFIR`의 항공기→섹터 배정(point-in-polygon, `pip()`→`geo.js`의 `pointInPolygon`으로 이식)·속도/방향 외삽(`extrap()`→`extrapolatePosition`, 대권 공식 동일)·`trafficGrade()`(7/12 임계, config화) 그대로 포팅. 원본은 인천/대구 ACC 토글 UI + 전체 섹터 상시 표시였지만, 이 저장소는 "결정 중심" 원칙(2026-07-23 확정 UI 방향)에 따라 **선택된 경로가 통과하는 섹터만** 표시하도록 범위를 좁혔다(`routeSectorIds()` 신규 — 완성본에도 있던 함수, 그대로 이식).
  - **범위를 좁힌 부분(정직하게 명시)**: 완성본 analyzeFIR의 레이더 기상 샘플링(dBZ)·회피(avoid) 판정은 이번 포팅에 미포함 — doc13 STEP A4 수용기준 3개(섹터별 현재/예측 대수·등급, 통과 섹터 표시, ADS-B 미가용 degrade)에 교통량만 명시돼 있고, 기상 결합은 A5(세그먼트 병목 종합, A1·A3·A4를 묶는 단계)의 몫으로 미뤘다. 유사호출부호 쌍은 사용자가 이번 라운드 범위에서 이미 명시적으로 제외한 별도 항목(위 "보류" 목록)이라 포함하지 않음.
  - **완성본 재현 확인 — GH/GL "죽은 섹터" 함정 그대로 보존**: 원본 데이터에서 GH(Gunsan High)와 GL(Gunsan Low)이 완전히 동일한 폴리곤을 공유하는데, `pip()` 배정이 배열 순서상 첫 매치에서 멈추는 원본 로직 특성상 GL은 실제로는 절대 항공기를 받지 못한다(원본의 실제 동작, 버그가 아니라 완성본이 그렇게 동작함) — 이 저장소도 seq 컬럼으로 원본 배열 순서를 DB까지 보존해 동일하게 재현(합성 스냅샷 단위테스트로 확인).
  - **단위 테스트(순수함수, 합성 입력)**: `extrapolatePosition`(gs<50/gs·track 결측 시 null, 정북 60분 외삽 좌표 검증)·`assignSector`(공유 폴리곤 GH 우선 배정, 폴리곤 밖은 null)·`trafficGrade`(7/12 경계값)·`sectorDemand`(8대 배정·등급·결정성 재현)·`routeSectorIds`(중복 제거·순서 보존) 18개 어서션 전부 통과. 완성본 알고리즘이 확정 알고리즘(analyzeFIR)이라 golden 대조 가능 — A1/A2와 동일 수준 검증.
  - **버그 발견·수정(자체 검토 중)**: 프로젝트 컨벤션(`frontend/js/adapters.js` — API는 snake_case, 프론트 내부는 camelCase)을 처음에 놓쳐 `s.sectorId`가 실제 응답 필드 `sector_id`와 어긋나 있었다 — `assignSector`가 항상 `undefined`를 반환해 `routeSectorIds`가 항상 빈 배열이 되는 조용한 버그였음(단위테스트는 합성 fixture라 이 어댑터 계층을 안 거쳐 통과했었고, 실제 API 응답으로 브라우저 종단 확인을 하다가 발견). `adapters.js`에 `toAccSector()` 추가해 수정, 실제 RKSI→RJBB 경로로 재검증(WH 섹터로 정상 배정 확인).
  - uvicorn+curl 종단 확인: `/api/reference/acc-sectors` 14섹터 전체 반환(seq 순서·GH/GL 순서 원본과 일치), 기존 `/api/reference/firs`·`/api/routes/od-pairs` 회귀 없음.
  - Playwright 실제 브라우저 확인: RKSI 출발 노선 선택 → 경로 옵션(RKSI→RJBB) 클릭 → `#sector-panel`에 "West-sea High Sector (WH) / 36 · 혼잡 / +10분 24 ▼" 정상 표시(실제 ADS-B 데이터로 36대 배정·혼잡 등급·10분 후 감소추세 전부 정상). 외부 ADS-B API가 CORS 차단/레이트리밋될 때는 "실시간 교통 데이터 없음(ADS-B 미가용)"으로 조용히 degrade(수용기준 (3) 확인), 경로가 한국 ACC 권역을 안 지나면 "경로가 지나는 섹터 데이터 없음" 명시(허위 정보 생성 금지 원칙).
  - `code-reviewer` 리뷰 통과 — 3건 발견·전부 수정: **High** `loadSectors()`가 실패한 조회 프로미스까지 영구 캐시해 일시적 503/네트워크 오류 1회로 섹터 패널이 새로고침 전까지 영구 고장 상태가 되던 버그 → 실패 시 캐시를 비워 다음 호출이 자연 재시도하도록 수정. **Medium** `update()`가 `++seq` 직후 `currentSectorIds`를 즉시 비우지 않아, `loadSectors()` await 도중 도착한 ADS-B 스냅샷이 잠깐 이전 경로의 섹터로 렌더될 수 있던 경쟁조건 → seq 증가 직후 동기적으로 비우도록 수정. **Low** `name_en`이 DB에서 nullable인데 폴백 없이 그대로 보간해 `"null (XX)"`로 표시될 수 있던 것 → `d.nameEn ?? d.sectorId`로 수정. 수정 후 18개 단위테스트 전부 재통과 확인.
- [x] A5 세그먼트 병목 신호 종합(1단계 부분힌트, 2026-07-24 완료) — `frontend/js/route-bottlenecks.js`
  - **범위(정직하게 명시)**: doc11 §17.3의 완전한 세그먼트 병목(교통×기상×경로교차)은 §13(교통 이력 프로파일)·§15①(구간별 상층풍)·④(SIGMET/PIREP 루트교차)이 선행돼야 하는 후속 과제(문서 자신이 명시)라 이번엔 손대지 않음 — 대신 **이미 검증된 A1(흐름관리 영향률)·A3(경로 상층풍/시어)·A4(섹터교통)를 그대로 모아 구조화 배열로 산출**하는 순수함수 `routeBottlenecks(context)`만 구현. 새 임계값을 발명하지 않고 A3의 `catGrade`·A4의 `trafficGrade`가 이미 가진 등급만 재사용(약함 이하/원활 등급은 제외해 노이즈 방지).
  - **doc11이 제안한 4번째 신호("FIR별 흐름관리 유효창")는 의도적으로 제외**: `/api/flow-management`는 과거 배치 이력일 뿐 "지금 발효 중"을 판정할 실시간 근거가 없다 — 없는 실시간성을 지어내지 않음(허위 정보 생성 금지 원칙).
  - A1·A3은 경로 전체 단위 신호라 `fir: null`(특정 FIR 귀속 근거 없음), A4만 유일하게 실제 FIR(RKRR)에 귀속.
  - **연동 리팩터(부수 개선)**: `windLayer`/`sectorPanel`에 각각 `getRecommendation()`/`getDemand()` getter 추가(캡슐화는 유지, 최소 요약만 노출). 이 과정에서 `analyze-sectors.js`의 `recompute()`가 원래 fire-and-forget(`loadSectors().then(...)`, 미await)이라 `update()`가 반환된 뒤에도 렌더링이 끝났다는 보장이 없던 것을 발견 — `recompute()`를 async로 바꿔 `update()`가 `await`하도록 수정(A5가 `getDemand()`를 안정적으로 읽기 위한 선행 수정이자, 그 자체로 잠재적 경쟁조건 제거).
  - **단위 테스트(순수함수)**: `routeBottlenecks` 17개 어서션(신호 없음→빈 배열, `found:false`/약함 시어/원활 섹터 필터링, 등급별 severity 매핑, `nameEn` 결측 폴백, 3신호 결합 순서, 결정성) 전부 통과.
  - Playwright 실제 브라우저 확인: RKSI→RJBB 선택 → `#route-bottlenecks`에 "흐름관리 영향률 2%(21/1209편, 추정치)" 정상 표시, 이번 표본은 시어 약함(2.7kt/1000ft)이라 wind_shear 항목 정상 생략, ADS-B 스냅샷이 아직 안 도착한 시점엔 섹터 신호도 정직하게 생략(수치 지어내지 않음). "전체 겹쳐보기" 선택 시 패널 정상 숨김.
  - `code-reviewer` 리뷰 통과 — High 1건·Medium 1건·개선 1건 수정, Minor 2건은 문서화만: **Critical→High로 재분류해 수정**: `main.js`의 `Promise.all([windLayer.update,sectorPanel.update]).then(()=>bottlenecksPanel.update())`에 세대 토큰이 없어, 노선을 고르자마자 바로 선택 해제하면(`bottlenecksPanel.clear()` 먼저 실행) 뒤늦게 끝난 Promise.all이 이미 숨긴 패널을 다시 그릴 수 있었음 → `sidStarSeq`와 동일한 패턴으로 `bottlenecksSeq` 추가(선택·해제 양쪽에서 증가), `.then()` 진입 시 재확인 + `.catch(()=>{})`로 windLayer/sectorPanel 예외 시 조용히 건너뜀. **High**: `/api/routes/flow` 조회 실패를 "신호 없음"과 구분 못해 503/네트워크 오류가 "이 노선은 흐름관리 영향 없음"으로 잘못 보일 수 있었음(`route-flow-summary.js`가 이미 겪은 문제와 동일 유형) → 실패 사유를 별도 `bottleneck-warn` 행으로 명시. **개선(Improvement)**: A5 패널이 선택 시점에 1회만 렌더돼 그 뒤 ADS-B 폴링으로 섹터 교통이 바뀌어도 반영 안 됐음 → `bottlenecksPanel.refreshSectorSignal(dep,arr)` 추가, `adsb.onSnapshot` 콜백에서 `sectorPanel.onAircraftUpdate` 직후 호출(A1 재조회는 안 함, A4 표시만 최신화). **Minor(문서화만, 미수정)**: `catGrade`를 `wind.js`(모듈 최상단에서 `window.L` 참조)에서 import하는 결합도 — 기능 결함은 아니고(A3도 이미 같은 구조) 향후 종합 코드가 늘어나기 전에 중립 모듈로 뺄 만한 리팩터 후보로만 기록. 수정 후 17개 단위테스트·Playwright 재검증 전부 통과.
- [x] A6 터미널 신호 이식(진출입 게이트·출발 활주로 분포, ODR2 `odInfo`→`/api/routes` 노출, 2026-07-24 완료) — D20 확정
  - **계산 로직 출처(A1/A2와 동일한 갭)**: 완성본 `odInfo`(진출입 게이트·활주로 분포)를 만드는 계산 스크립트도 이식 패키지 어디에도 없었다(3_agg_csv.py·4_build_routes2.py·5_build_map3.py 전부 확인 — 마지막 것은 사실 완성본 HTML 자체를 그대로 담은 참고 파일이라 계산이 아니라 소비 로직뿐). 대신 `processed_flight_data`에 **이미 이 값이 그대로 있었다** — `entry_fir`/`exit_fir` 컬럼(컬럼명과 달리 실측상 FIR 코드가 아니라 5자 픽스명, 예: RKSI→RJBB는 `KARBU`/`LANAT`로 전 표본 일관)과 `processed_acdm_departure.runway`(신규 조인 — A1이 `processed_acdm_arrival`에 시도했던 callsign=flight_icao 키 패턴에서 착안했으나 그 코드 자체를 재사용한 건 아님, 리뷰 지적으로 실제 코드에 없는 전례를 있다고 쓴 주석을 바로잡음). 새 알고리즘을 발명한 게 아니라 이미 존재하는 물리 컬럼 2개를 그대로 노출.
  - `code-reviewer` 리뷰 통과 — Correctness 2건 수정, 문서 정확성 1건 수정: **①** `_fetch_acdm_departure_runways()`의 조인 키가 `(callsign, date)`뿐이라 같은 편명이 같은 날 다른 공항에서 출발하면(회항 후 재출발 등) 서로 다른 두 항공편의 활주로가 뒤섞일 수 있었음 → `airport_icao`(dep)를 키에 추가한 3중 키로 수정, 재검증(RKSI→RJBB 매칭률 99.9% 동일 유지, 회귀 없음). **②** 그 조회에 `ORDER BY`가 없어 동률 키(수정 후엔 사실상 안 생기지만 방어적으로) 발생 시 dict 덮어쓰기 승자가 실행마다 달라질 수 있었음 → `id` 순 정렬 추가(`_fetch_flight_data_rows()`와 동일한 이유), try/except 래핑도 그 함수와 동일하게 추가. **③** 주석/문서가 "A1/A2가 이미 쓴 조인 키를 재사용"이라고 썼는데 실제로는 그런 코드가 저장소에 없음(A1의 해당 시도는 검증 실패로 폐기됨, 리뷰가 근거 확인 요청) → 위처럼 정확하게 정정.
  - **위치는 D20 확정대로 OD 단위가 아니라 경로그룹 단위**([14 §8 C-2](./14-improvement-request.md)) — `advisor_odr2_route`에 `gate_in`/`gate_out`(경로그룹 최빈값) 스칼라 컬럼 2개 추가(마이그레이션 `c3e7a1f6b0d4`), 출발 활주로 분포는 리스트라 이 저장소의 완전 정규화 원칙(`d4f7a91c3e26` 결정)을 그대로 따라 JSONB 블롭 대신 `advisor_odr2_route_fir`/`_route_fix`와 동일한 모양의 자식 테이블 `advisor_odr2_route_runway` 신설(같은 마이그레이션). **시행착오**: 처음엔 `runway_dist JSONB` 컬럼으로 시도했다가 SQLAlchemy가 타입 미지정 컬럼에 Python list를 바인딩할 때 Postgres ARRAY로 오인식해 INSERT가 깨짐(`invalid input syntax for type integer: "33L"`) — 이 저장소의 기존 아키텍처 결정(완전 정규화, 서로게이트 id 없음)과도 맞지 않아 자식 테이블로 재작성.
  - `backend/batch/build_odr2.py`: `aggregate()`가 `entry_fir`/`exit_fir`/callsign→runway를 그룹별로 집계(최빈값/분포%, `fr`(대표 full_route) 최빈값 계산과 동일한 패턴 재사용). `queries/routes.py:routes_for()`가 `gate_in`/`gate_out`/`runway_dist`를 옵션 객체에 노출.
  - 프론트: `adapters.js:toRouteOption()`에 `gateIn`/`gateOut`/`runwayDist` 추가, `route-panel.js`가 경로 카드에 "인천 진출입 : 진입 X → 진출 Y · 출발 활주로 A % · B %"로 표시(완성본 `castScript`의 "진입 X, 진출 Y를 사용하는 경로" 서술과 동일 입력, 둘 다 없으면 조용히 생략).
  - uvicorn+curl 종단 확인: RKSI→RJBB 3개 경로그룹 전부 `gate_in`/`gate_out`/`runway_dist` 정상 반환(예: 경로1 `KARBU`/`LANAT`/`[[33L,78],[15R,22],[34R,1]]`), 재실행해도 OD 1,487·경로그룹 3,083 동일(회귀 없음), 기존 `/api/routes/od-pairs` 정상.
  - Playwright 실제 브라우저 확인: RKSI→RJBB 선택 시 경로 카드 3개 전부에 "인천 진출입 : 진입 KARBU → 진출 LANAT · 출발 활주로 33L 78% · 15R 22% · 34R 1%" 형태로 정상 표시, 콘솔 에러 없음(무관한 404 1건 제외).
- [x] A7 SUAS/MOA 통과시각 발효 판정(신규, D23, 2026-07-24 완료) — 선행 2건 모두 충족: **①** SUAS 참조 레이어(`/api/reference/suas`)는 별도 세션이 동시에 구현(`reference_suas` 테이블·`toSuas` 어댑터·지도 렌더링), 이 STEP은 그 위에 발효시간만 추가 — 겹치지 않는 부분이라 손대지 않고 그대로 사용. **②** `EFF_TIMES` 보존은 `backend/batch/build_suas.py`(신규)가 담당.
  - **완성본에도 없는 신규 파생(doc13이 명시)**: 완성본 빌드조차 DAFIF `SUAS_PAR.TXT`의 `EFF_TIMES`를 `suas.json` 생성 단계에서 버렸다 — 원본을 직접 읽어 파싱. 스키마 소유는 `reference_*`(별도 세션의 Stage 0 이관)가 아니라 `advisor_*`(신규 테이블 `advisor_suas_schedule`, 마이그레이션 `d9f2b4a8c1e6`) — `ident` 문자열로 애플리케이션 레벨 조인(`reference/loader.py:load_suas`), 스키마 소유가 갈려 있어 별도 세션의 테이블 정의를 건드리지 않는다.
  - **안전 우선 파싱(창작·억측 금지)**: 요일 키워드(MON~SUN 단일/범위, DLY/DAILY/CONT=매일)+UTC 시간범위(HHMM-HHMMZ, 콤마로 여러 구간)만 구조화 파싱(`status:"structured"`). `SR`/`SS`(일출몰)·`NOTAM`·`HOL`(공휴일 예외 — 실제 날짜 검증 불가)이 포함되거나 정규식이 못 알아보면 전부 `status:"confirm_required"`로만 표시, 발효 여부 단정 안 함. 실측: DAFIF 18,426건(중복 61건 제외 18,344 유니크 ident) 중 구조화 5,145·확인필요 13,199.
  - **수용기준 (1) 확인**: doc13이 예시로 든 한국 ASAN(`(RK)A2`, `MON-FRI 2300-1300Z, SAT 2300-0400Z`) 실측 파싱 결과가 정확히 `[{days:[MON..FRI],utc_start:2300,utc_end:1300},{days:[SAT],utc_start:2300,utc_end:400}]` — 요일·자정랩어라운드 포함 단위테스트로 발효/비발효 정확히 판정 확인.
  - **시행착오(A6 runway_dist와 같은 원인, 재발)**: `segments`(중첩 리스트-of-딕트)를 타입 없는 `sa.table()`/`sa.column()` 프록시로 INSERT하니 이번엔 `can't adapt type 'dict'`로 즉시 실패(리스트 때는 ARRAY로 오인식했지만 딕트는 아예 어댑터가 없어 더 명확하게 터짐) → `reference_tables.py`처럼 명시적 JSONB 타입의 `sa.Table` 객체로 교체해 해결.
  - 프론트: `frontend/js/route-bottlenecks.js`(A5) 확장 — 순수함수 `isSuasActiveAt`(요일+HHMM 발효 판정, 자정랩 처리)·`findSuasCrossingEtaMin`(경로-폴리곤 교차 + 통과 예상시각, A3 `ptDist`·순항 TAS 가정 재사용)·`suasBottleneckItems`(교차+스케줄 결합, `kind:"airspace"`). **버그 발견·수정(자체 검토 중)**: 처음엔 경로 좌표(이름 붙은 픽스만 담은 성긴 배열) 자체만 폴리곤 판정해 두 픽스 사이에 통째로 들어간 SUAS를 놓쳤음(실제 RKSI→RKPC로 재현 확인, 0건→수정 후 2건) → 구간마다 10등분 보간 후 판정하도록 수정. 알려진 한계(미수정, 문서화만): 경도 단순 선형보간이라 날짜변경선을 넘는 구간에서는 부정확할 수 있음(SUAS가 한국·인접국 중심이라 실질 영향 낮음).
  - **수용기준 (2)(3) 확인**: `confirm_required`는 항상 "확인 필요"로만 표시하고 활성/비활성 단정하지 않음(단위테스트로 확인) · 발효시간 배치를 안 돌렸으면(`scheduleStatus` null) 해당 SUAS는 조용히 건너뜀(에러 아님).
  - **단위 테스트(순수함수)**: Python `parse_eff_times` 15개 어서션(ASAN 예시·SR-SS/NOTAM/HOL 비정형·CONT/DLY/DAILY·요일없는 시간범위·미기재·비정형 요일토큰·결정성) + JS `isSuasActiveAt`/`findSuasCrossingEtaMin`/`suasBottleneckItems` 20개 어서션(자정랩 경계값·폴리곤 교차/미교차·확인필요 항목 보존·스케줄 없는 SUAS 생략·미교차 SUAS 생략·결정성) 전부 통과.
  - uvicorn+curl 종단 확인: `/api/reference/suas?region=kr` ASAN 항목에 `eff_times_raw`/`schedule_status`/`schedule_segments` 정상 반환, 기존 지오메트리 필드·`/api/routes/od-pairs` 회귀 없음.
  - Playwright 실제 브라우저 확인: RKSI→RKPC 선택 시 `#route-bottlenecks`에 "SEOSAN TFR((RK)A20T) 통과 예정(약 0분 후) — 발효시간 확인 필요(비정형: MON-SAT)" 정상 표시(이 SUAS는 요일만 있고 시간범위가 없어 정직하게 확인필요로 분류됨).
  - `code-reviewer` 리뷰 통과 — **Critical 2건**(둘 다 자정랩 날짜 귀속 버그) 발견·수정: **①** `isSuasActiveAt`이 자정을 넘는 구간(`utc_start>utc_end`)의 저녁·새벽 두 쪽을 모두 "오늘이 days에 있으면 활성"으로 판정해, 새벽 쪽이 실제로는 **어제** 저녁부터 이어지는 것임을 놓쳤다 — 거짓양성(월요일 00:30을 활성으로 오판, MON-FRI엔 일요일 밤 활성이 없는데도)과 거짓음성(일요일 02:00을 비활성으로 오판, 실제로는 토요일 밤 SAT 2300-0400의 연장인데도) 둘 다 발생 — 후자가 "실제로 활성인데 안전하다고 잘못 알려주는" 더 위험한 방향. 저녁 쪽은 오늘, 새벽 쪽은 어제가 days에 있어야 활성이도록 수정, 요일 경계 14개 케이스로 재검증. **②** Python `parse_eff_times`가 콤마로 나열된 여러 시간대 중 요일 생략 구간(예: `"MON-FRI 0600-0900Z, 1300-1600Z"`의 두 번째 구간)을 무조건 "매일"로 채워, 원문이 명시하지 않은 주말 활성을 구조화 결과에 지어내던 버그(doc13 안전 우선 원칙이 명시적으로 금지하는 "거짓 구조화" 방향) → 요일 생략 구간은 직전 구간의 요일을 물려받도록 수정(맨 첫 구간이 생략이면 기존대로 매일 — 실측 패턴 "2100-1300Z" 단독 케이스는 그대로 유지). **Medium 2건**: 구간 보간 스텝이 고정 10개라 성긴 구간(대양 횡단 등)이 길면 작은 SUAS를 여전히 놓칠 수 있음 → 5NM당 1스텝(최소 10·최대 400)으로 구간 길이 비례 조정. `_persist()`가 파싱 결과 0건이어도 TRUNCATE부터 실행해 기존 정상 데이터를 조용히 날릴 수 있음 → 0건이면 적재 자체를 중단하는 명시적 오류로 변경. **Low 1건**: `DLY`/`DAILY` 단독(시간범위 없이 요일 키워드만)이 `CONT`와 달리 특례 처리가 안 돼 있어 확인필요로 새던 것 → 동일하게 매일 24시간 구조화로 처리(실측 재실행 결과 구조화 5,145→5,149건, 확인필요 13,199→13,195건 — DAFIF 실제 데이터에 이 패턴이 4건 존재했음을 확인). 수정 후 Python 16개(day-inheritance 회귀 케이스 추가)·JS 27개(요일 경계 14개로 확장) 단위테스트 전부 재통과, 실제 DAFIF 재적재·uvicorn 종단·Playwright 재확인 전부 정상.

### Phase A(A4-A7) 통합 리뷰 (2026-07-24)

개별 항목 리뷰(위 각 항목의 `code-reviewer` 통과)와 별개로, 네 항목이 실제로 한 화면(`#route-bottlenecks`)에서 **함께** 동작할 때만 드러나는 문제를 `senior-code-reviewer`로 종합 점검.

- **확인 2건(Confirmed) — 둘 다 시간 순서 버그**: **①** `route-bottlenecks.js`의 `update()`가 `lastFlowImpact`/`lastFlowError`는 `await` 전에 동기적으로 비우면서 `lastSuasItems`는 안 비워, 노선을 빠르게 다시 선택하면 흐름관리·상층풍·섹터교통은 이미 새 노선 것인데 SUAS 경고만 이전 노선 것이 잠깐 섞여 나올 수 있었다(리뷰가 코드 추적 + 재현으로 확인) → `lastSuasItems`도 동일하게 `await` 전에 비우도록 수정. **②** `main.js`의 `adsb.onSnapshot` 콜백이 `sectorPanel.onAircraftUpdate(aircraft)`를 `await` 없이 호출한 직후 곧바로 `bottlenecksPanel.refreshSectorSignal()`을 불러, 매 ADS-B 폴링(12초)마다 병목 패널의 섹터 교통 신호가 항상 한 주기 뒤처진 값을 보여주고 있었다(`analyze-sectors.js`의 `recompute()`는 이미 async였는데 `onAircraftUpdate`가 그 프로미스를 반환하지 않아 fire-and-forget이 되던 것) → `onAircraftUpdate`가 `recompute()`의 프로미스를 반환하도록 수정 + `main.js`가 `await`하도록 수정. 수정 후 RKSI→RJBB 재검증: `#sector-panel`과 `#route-bottlenecks`의 섹터 교통 수치가 정확히 일치함을 확인(수정 전에는 이론상 한 주기 어긋날 수 있었음).
- **문서 미비 1건(Plausible)**: A6(터미널 신호: 진출입 게이트·출발 활주로 분포)이 A1/A3/A4/A7과 달리 `routeBottlenecks()`에 결합돼 있지 않은데, 이게 의도적 범위 결정인지 문서 어디에도 명시가 안 돼 있었다 — A6는 "병목·위험 등급"이 아니라 경로 자체의 사실 정보(이미 `route-panel.js`가 경로 카드에 항상 표시)라 성격이 달라 의도적으로 제외한 것인데, 이 판단이 코드에만 있고 문서화가 안 돼 향후 담당자가 "빠뜨린 것"으로 오해할 소지 → `route-bottlenecks.js` 모듈 docstring에 제외 사유 명시.
- **재확인(문제없음)**: 노선 재선택 시 `bottlenecksSeq` 세대 토큰이 `windLayer`/`sectorPanel` → `bottlenecksPanel` 핸드오프 전 구간에서 올바르게 합성됨(경쟁조건 없음) · A7(SUAS 교차)은 노선 선택 시 1회만 계산돼 캐시되고 ADS-B 폴링마다 재계산되지 않음(불필요한 반복 계산 없음) · DB 마이그레이션 체인 드리프트 없음(`alembic current`=`alembic heads`) · `column_map.py` 화이트리스트가 실제 생성된 컬럼과 전부 일치 · 신규 SQL·DOM 삽입 지점 전부 안전(파라미터라이즈드 쿼리·`escapeHtml`/`textContent`) · 체크리스트가 인용한 수치(ASAN 파싱 결과, DAFIF 18,426/18,344/5,149/13,195건, `routes_for`/`od_pairs` 실측값)를 전부 재실행으로 재현해 실제와 일치 확인(허위 기재 없음).

**Phase B — 통합기 (reasoning context)**
- [ ] B1 `reasoningContext` 스키마 v1.0 확정([13 §7](./13-ai-reasoning-dev-plan.md)) — 민감도 태그 `public`/`masked`/`restricted`(§7.1) 포함
- [ ] B2 context 어셈블러 순수함수 — `frontend/js/reasoning-context.js`
- [ ] B-exit Phase B→C 전환 기준 충족 확인([13 §5.1](./13-ai-reasoning-dev-plan.md): 스키마 v1.0·필드 충족·결측 null·민감도 태그·순수성)

**Phase C — LLM 서술층 (얇은 층, D1 수동복붙)**
- [ ] C1 프롬프트 템플릿 + 골든예시(완성본 `castScript` 재활용) + 출력 JSON 스키마(D6)
- [ ] C2 "AI 근거 보기" 버튼(D5) + 프롬프트 복사 + 응답 붙여넣기 UI — `frontend/js/reasoning-panel.js`
- [ ] C3 응답 검증·파싱·안전렌더 (스키마 검증·textContent·XSS 방어 — 신뢰 불가 입력)
- [ ] C4 모드 C 백엔드 프록시 스켈레톤(**미활성** D9, 키 env·요청량 제한·로그 규약만)

### 알려진 이슈 → 수정 완료 (2026-07-23)
- [x] `frontend/js/main.js`의 `focusAirportsFor()`가 boot 시 가져온 민간/공용(A/B) 타입 공항만 필터링 — OD의 출발/도착 공항이 군용/기타(C/D) 타입이면 결정 포커스 모드에서 마커가 조용히 사라지던 문제(2026-07-23 2차 리뷰에서 발견, flow-management 작업과 무관해 당시 미수정). 실측으로 실재 확인: RKSO(Osan AB, type C) 등 391개 실제 OD 페어의 dep/arr 44종이 부트 A/B 목록에 없었음.
  - 수정: `backend/app/reference/loader.py:load_airports()`·`routers/reference.py`에 `icao` 파라미터 추가(`load_firs`와 동일 규약 — 있으면 bbox/type 완전히 무시). `frontend/js/api.js:airports()`가 `icao` 전달, `frontend/js/store.js`에 `ensureAirportsLoaded(icaos)` 추가해 `selectOd()`에서 `loadFocusReference`와 병렬로 dep/arr 누락분만 타입 무관 보강 조회, `derived.airportByIcao`/`bootAirports`에 병합(중복 fetch/push 방지용 `pendingAirportIcaos` 가드 포함).
  - `senior-code-reviewer` 리뷰 1회 진행, Medium 1건 수정: 라우터의 `type` Query가 `pattern` 검증을 icao 유무와 무관하게 먼저 걸어 "icao 있으면 type 무시" 규약이 `type` 형식 자체엔 안 지켜지던 것 발견 → Query pattern 제거하고 loader 내부(icao 없을 때만 실행되는 분기)에서만 검증하도록 이동, `bbox`와 동일 패턴으로 통일. 문서 동기화([03 §3](./03-backend-api.md) 표에 `icao` 필터 추가) 함께 처리.
  - uvicorn+curl 종단 확인: A/B 회귀(3,457건 동일)·군용 단건 조회(RKSO type=C 정상 반환)·icao+bbox/무효type 동시 지정 시 icao 우선(200)·icao 없이 무효 type만(400, 회귀 유지). Node로 실제 `store.js`/`api.js`를 백엔드에 붙여 `selectOd("RKSO","RKPK")` 실행 후 `focusAirportsFor` 결과에 RKSO 포함 확인, 재조회·동시 요청 2종 모두 중복 push 없음 확인.

### 사용자 요청 반영 (2026-07-23) — 지역컨텍스트 FIR 클릭 시 항로 표시
- [x] **문제**: `main.js`의 `relevantFirBounds()`가 지역컨텍스트(bulk) 뷰에서 항로·항행시설·픽스를 홈 FIR(RKRR)+선택된 경로의 경유 FIR로만 제한한다(번잡함 방지, 기존 설계). OD(노선)를 아직 선택하지 않은 상태에서는 ZSHA(상하이)·RCAA(타이베이)·RJJJ(후쿠오카) 같은 외국 FIR 쪽엔 항로가 전혀 안 보여서 사용자가 지도를 훑어보다 혼동하는 상황 확인(스크린샷 제보).
- [x] **수정**: FIR 폴리곤을 클릭(팝업이 열리는 동작)하면 그 FIR의 ICAO를 `main.js` 내부 `pinnedFirIcaos`(Set, 뷰 전용 상태)에 추가하고 `relevantFirBounds()`가 이를 홈 FIR·경로 FIR과 함께 포함하도록 함(`frontend/js/main.js`, `frontend/js/layers/reference.js`의 `createReferenceLayers(map, CONFIG, { onFirClick })` hook 추가). 팝업이 닫히지 않도록 FIR/공항 레이어는 다시 그리지 않고 항로·항행시설·픽스만 재필터링하는 `applyBulkOverlayFilters()`로 분리.
- [x] `code-reviewer` 리뷰 통과 — 클릭 핸들러 결선(`onFirClick` → hooks → `layer.on("click")`), `applyBulkOverlayFilters`의 함수 선언 호이스팅, 결정 포커스 모드에서 클릭 시 즉시 반영 안 되고 지역컨텍스트 전환 시 자동 적용되는 동작, `pinnedFirIcaos`(최대 247개 문자열) 메모리 영향 없음, 기존 `renderForCurrentState()` bulk 분기 동작 회귀 없음, escapeHtml 경계 미변경(새 DOM 삽입 없음) 전부 확인. Critical/High 없음.
- [x] `node --check`로 `main.js`/`layers/reference.js` 구문 확인.

### 사용자 요청 반영 (2026-07-23) — 초기 부팅 기본값: 우리나라 FIR·항로·실시간 항공기
- [x] **문제**: `store.js`의 `bootMinimal()`이 `loadFocusReference([])`로 우리나라(홈 FIR, RKRR) FIR·경계 안 항공로·픽스를 이미 계산해 `state.focusFirs`/`focusAirways`/`focusWaypoints`에 담아두는데, `main.js`의 `boot:ok` 핸들러가 `renderAirports()`만 호출하고 `renderFirs`/`renderAirways`/`renderWaypoints`를 호출하지 않아 첫 화면이 빈 지도로 보이던 버그(사용자 스크린샷 제보). 또한 지도가 `CONFIG.map.center`(세계뷰, zoom 3) 그대로 열려 우리나라로 fit되지 않아, 노선 미선택 시 지도 중심 기준으로 폴링하는 실시간 항공기(`layers/adsb.js`)도 엉뚱한 위치를 조회하고 있었음.
- [x] **수정**: `main.js`의 `boot:ok` 핸들러가 기존 `renderForCurrentState()`(다른 이벤트에서 이미 쓰던 헬퍼)를 호출하도록 변경 + 홈 FIR 폴리곤 bounds(`geo.js`의 `boundsOfPolygons`, Leaflet corner-pair 포맷으로 변환)로 `map.fitBounds()` 추가. `adsb.start(chipEl)` 호출 시점을 `bootMinimal()` 완료 이후로 이동해 첫 폴링부터 우리나라 FIR로 맞춰진 지도 중심을 기준으로 조회하도록 함(이전엔 `bootMinimal` 이전에 시작해 첫 폴링만 옛 세계뷰 중심으로 나가던 문제).
- [x] `code-reviewer` 리뷰 통과 — `boundsOfPolygons`→`fitBounds` 포맷 변환 정확성, `boot:ok` 시점에 `state.derived.firByIcao` 채워짐 보장(레이스 없음), `renderForCurrentState()`가 기존 부팅 동작(공항 렌더)의 상위집합임을 확인. 리뷰 지적사항 2건 반영: ①ADS-B 첫 폴링이 fitBounds 적용 전 발생하던 문제(위 수정으로 해소) ②새 렌더/fitBounds 블록에서 예외 발생 시 `bootMinimal`의 catch로 잘못 전파돼 "초기 데이터 로드 실패" 토스트가 오표시되는 것을 막기 위해 자체 try/catch로 격리.
- [x] `node --check`로 `main.js` 구문 확인.

### 사용자 요청 반영 (2026-07-23) — 항공기 클릭 시 지나온/도착 예상 경로 표시
- [x] **요청 배경**: 항공기 클릭 팝업(고도·속도·노선)에 추가로 "지나온 경로"·"도착 경로"도 지도에 표시해달라는 요청. 조사 결과 무료 ADS-B API(`adsb.lol`, `adsbdb.com`) 어디도 개별 항공편의 실제 웨이포인트 항로는 주지 않고 출발/도착 공항 코드까지만 준다(`PORTING_PACKAGE_ROOT`의 완성본 HTML도 `adsb.lol/api/0/route/{callsign}`를 동일하게 공항코드 조회 용도로만 씀, 확인 후 사용자에게 보고). 사용자 확인: 실제 항로 대신 이 앱 백엔드의 경로추천(ODR2, `/api/routes`) 데이터를 재활용하기로 결정 — 실측으로 확인한 실제 사례(ZBAA→RKSI 289편)처럼 advisor DB에 있는 OD 쌍이면 이 경로를 근사치로 사용.
- [x] **구현**(`frontend/js/layers/adsb.js`): 항공기 클릭 시 `showAircraftDetail`에서 adsbdb로 얻은 출발/도착 ICAO로 `api.routes(dep,arr)` 조회 → 옵션의 `full_route_coords`(없으면 `track_coords`)를 현재 항공기 위치에서 가장 가까운 지점 기준으로 분할해 지나온 구간(실선)·도착 예상 구간(점선, `routeGuessGroup` 레이어)으로 그림. advisor DB에 없는 OD 쌍(세계 임의 항공편, 대다수)이면 조용히 생략 — 지어내지 않음. 팝업에 "실제 항로가 아니라 경로추천 데이터 기반 추정치"임을 명시. 팝업을 닫거나 다른 항공기를 클릭하면 이전 추정선을 지움(`routeGuessSeq` 세대 토큰 + `popup.on("remove")`).
- [x] `code-reviewer` 리뷰 통과 — Critical 1건 발견·수정: 날짜변경선을 넘는 노선(한국↔미국 등, 이 파일이 `unwrapLongitudes`를 쓰는 바로 그 이유)에서 분할 지점(`nearestIndex`)을 언랩 후 좌표로 계산해 노선 반대쪽 끝으로 튀던 버그 → 언랩 전(raw, ADS-B 원시 경도와 동일 convention) 좌표로 계산하도록 수정, node 스크립트로 재현·해소 확인. 그 외 지적사항 1건도 반영: 팝업을 닫아도 진행 중이던 조회가 늦게 도착하면 설명 없이 경로선만 남던 문제 → `remove` 핸들러에서 `routeGuessSeq`도 함께 무효화. XSS(escapeHtml 경계)·예외처리(advisor DB에 없는 OD 쌍의 404 등)는 문제없음 확인.
- [x] `node --check`로 `layers/adsb.js` 구문 확인.

### 사용자 요청 반영 (2026-07-23) — 항공기 아이콘 헤딩 회전 + 편명/노선 상시 2줄 라벨, 조회 반경 축소
- [x] **요청 배경**: 참고 완성본(`PORTING_PACKAGE_ROOT`)처럼 항공기 아이콘을 헤딩 방향으로 회전하는 실루엣으로 바꾸고, 편명 아래 출발→도착 2줄 라벨을 항상 표시해달라는 요청. 이는 과거 결정(F8, "무료 adsbdb 부하 방지 위해 상시 라벨은 편명만")을 뒤집는 것이라 사용자에게 확인 후 진행: 완성본의 배치 조회(사이클당 편명 최대 12개, adsb.lol 우선·adsbdb 폴백) 방식을 그대로 이식해 부하를 상한 내로 유지하며 상시 2줄 라벨을 지원하기로 결정. 이어서 "실시간 항적 조회 반경이 너무 넓다"(250NM)는 피드백으로 100NM 축소도 함께 반영.
- [x] **구현**: `frontend/js/layers/adsb.js` — `PLANE_PATH`(완성본 canvas path를 SVG로 이식) 기반 `iconFor(track, isGround)`가 halo+fill+outline 2겹 SVG를 CSS `rotate(track deg)`로 회전(지상 기체는 회색). `routeCodes` 캐시(Set 기반 배치 수집) + `fetchRouteCodes()`가 폴링마다 신규 편명을 배치 크기만큼 조회, 완료된 것만 해당 마커 툴팁을 `buildLabelHtml()`(콜사인/hex + 노선, 전부 `escapeHtml`)로 즉시 갱신. `frontend/js/config.js`/`config.example.json`에 `tokens.aircraft*`(색상), `adsb.routeCodeLookupUrl/routeCodeBatchSize/routeCodeBatchDelayMs`, `adsb.radiusNm`(250→100) 추가. `frontend/css/style.css`의 `.aircraft-icon`을 이모지 스타일에서 SVG 래퍼 스타일로, `.aircraft-label-cs`/`.aircraft-label-route` 2줄 스타일 추가.
- [x] `code-reviewer` 리뷰 통과 — Critical 1건 발견·수정: 공용 `net.js`의 `fetchJsonWithFallback`이 타임아웃 없이 `fetch()`를 호출해, adsb.lol/adsbdb 중 하나가 응답 없이 걸리면 `routeCodesBusy` 뮤텍스가 영구히 풀리지 않아 이후 모든 폴링 사이클에서 라벨 2번째 줄 갱신이 세션 내내 정지하는 문제 → `CONFIG.netTimeoutMs`(8000ms, 완성본의 6000ms와 동일 취지) + `AbortSignal.timeout()`을 `net.js`에 추가(이 파일을 쓰는 weather.js/adsb.js의 다른 fetch 전체에도 동일하게 적용되는 방어). Minor 1건도 반영: `need` 배열 O(n²) 중복 제거를 `Set`으로 교체. 회전 방향(0deg=북쪽, 완성본 `ctx.rotate`와 동일)·`isGround` 판정 일치·`buildLabelHtml` 호출부 인자 일관성(콜사인만 캐시 키로 사용)·XSS(모든 외부 문자열 escapeHtml 경유)는 문제없음 확인.
- [x] `node --check`로 `layers/adsb.js`/`config.js`/`net.js` 구문 확인, `config.example.json` JSON 파싱 확인.

### 사용자 요청 반영 (2026-07-23) — 위험기상 지도 UX 3건(색상 충돌·픽스 좌표·PIREP 라벨)
- [x] **문제 3건 스크린샷 제보**: ① SIGMET(위험기상) 폴리곤이 `route.js`의 경로 경유 FIR 하이라이트·미니맵 뷰포트와 동일한 `CONFIG.tokens.orange`를 써서 둘 다 켰을 때 구분이 안 됨. ② 항로 픽스 클릭 팝업이 ident만 보여주고 지점 위경도가 없음. ③ 레이어 컨트롤의 "PIREP" 라벨이 무슨 뜻인지 알기 어려움(옆의 "SIGMET(위험기상)"과 달리 설명 없음).
- [x] **수정**: ① `frontend/js/config.js`(DEFAULT_CONFIG.tokens)·`config.example.json`에 `hazardRed: "#c92a2a"` 추가(단일 진실원 동기화), `frontend/js/layers/hazards.js`의 SIGMET `L.polygon` color/fillColor를 `CONFIG.tokens.orange` → `CONFIG.tokens.hazardRed`로 교체. ② `frontend/js/layers/reference.js`의 `renderWaypoints()` 팝업을 `escapeHtml(wp.ident)`만에서 `<b>ident</b><br>lat.toFixed(4), lon.toFixed(4)`로 확장(픽스 데이터엔 ident/lat/lon/country뿐, 별도 지점명 필드 자체가 없음 — `backend/app/reference/loader.py:load_waypoints` 확인). ③ `frontend/js/main.js`의 `L.control.layers()` PIREP 항목을 `"PIREP(조종사 기상 보고)"`로 변경(SIGMET과 동일 패턴).
- [x] `code-reviewer` 리뷰 통과 — `wp.lat`/`wp.lon`이 이 참조 레이어의 다른 렌더 함수(공항/항행시설)와 동일하게 null 가드 없이 신뢰 데이터로 쓰이는 기존 관례와 일치함(라이브 기상 피드인 hazards.js만 별도 타입 가드) 확인, `hazardRed` 토큰명 오타 없음(양쪽 파일 동기화 확인), 새 팝업 문자열 중 ident는 escapeHtml 경유·좌표는 숫자라 이스케이프 불요 확인. Critical/High 없음.
- [x] `node --check`로 `main.js`/`config.js`/`layers/reference.js`/`layers/hazards.js` 구문 확인, `config.example.json` JSON 파싱 확인.

### 사용자 요청 반영 (2026-07-23) — 결정 포커스에서 항로 픽스 미표시 버그
- [x] **문제**: 위 항목 ②에서 픽스 팝업에 좌표를 추가했지만, 사용자가 스크린샷으로 "지역컨텍스트에는 나오는데 결정포커스에는 안 나온다"고 재제보. 원인 확인: `main.js`의 `renderForCurrentState()` focus 분기는 `state.focusWaypoints`(경로 bbox 스코프 픽스, `store.js:loadFocusReference`가 2026-07-23에 이미 실제로 채우기 시작함)로 `renderWaypoints()`를 호출하는데, `reference.js`의 `showFocus()`가 여전히 옛 주석("waypoints/tca/navaids는 focus에서 항상 빈 배열", `focusWaypoints` 추가 이전 상태 기준)대로 `waypoints` 레이어 그룹을 항상 `removeLayer`로 지도에서 빼고 있었음 — 데이터는 그려지는데 그룹 자체가 숨겨져 아무것도 안 보이고, 당연히 클릭할 마커도 없어 좌표 팝업도 못 봄(②의 재발처럼 보였지만 실은 이 버그의 결과).
- [x] **수정**: `frontend/js/layers/reference.js`의 `showFocus()`에서 `waypoints`를 `removeLayer` 목록에서 `addLayer` 목록(`firs`/`airways`와 함께)으로 이동. `tca`/`navaids`는 focus 분기가 여전히 `renderTca([])`/`renderNavaids([])`로 빈 배열만 넘기므로 그대로 숨김 유지. 관련 주석(§110·§214 인근) 최신화.
- [x] `code-reviewer` 리뷰 통과 — `renderWaypoints`가 `showFocus()` 호출 전에 실행돼 순서 문제 없음, `showBulk()`/줌가시성(`applyAirportZoomVisibility`)/라벨가시성(`updateLabelVisibility`)은 waypoints를 별도로 특별 취급하지 않아 영향 없음, 레이어 컨트롤 수동 토글과의 상호작용은 firs/airways와 동일한 기존 동작이라 회귀 아님 확인. Critical/High 없음.
- [x] `node --check`로 `layers/reference.js` 구문 확인.
- [x] **별도 확인(2026-07-23)**: 같은 스크린샷에서 "기상 레이더 체크 시 자동 반복 재생"도 문의받음 — 코드 확인 결과 체크박스 자체는 자동재생을 트리거하지 않고(`radar.js`의 `overlayadd`는 최신 프레임만 표시), ▶ 버튼을 누르면 프레임을 무한 루프 재생(`setInterval` + modulo 순환)하는 기존 설계. 사용자에게 의도 확인 결과 **"현재처럼 무한 반복" 유지 확정** — 코드 변경 없음.

### 사용자 요청 반영 (2026-07-23) — 결정포커스 기본값: 인천FIR 실시간 항적 고정 + 픽스 호버 툴팁
- [x] **요청 1**: "결정포커스 기본값에 인천FIR 실시간 항적을 계속 보여주는 것을 기본값으로 하자." 원인: `frontend/js/layers/adsb.js`의 `pollCenters()`가 노선 미선택 시 `map.getCenter()`로 폴백해, boot 시 인천FIR로 `fitBounds`해도 사용자가 지도를 패닝하면 ADS-B 조회가 조용히 다른 지역으로 따라가 버림.
  - **수정**: `adsb.js`에 `homeCenter` 상태 + `setHomeCenter(latlon)` 추가, `pollCenters()`가 `routeCoords` 다음 우선순위로 `homeCenter`를 쓰도록(그 다음이 기존 `map.getCenter()`). `main.js`의 `boot:ok` 핸들러에서 홈FIR `fitBounds` 직후 `adsb.setHomeCenter({lat, lon})` 호출(FIR `label` 좌표, 없으면 bbox 중심으로 폴백).
  - `code-reviewer` 리뷰 통과 — `boot:ok`가 `bootMinimal()` 내부에서 동기 호출돼 `adsb.start()`(bootMinimal 완료 후)보다 항상 먼저 실행됨 확인(첫 폴링부터 homeCenter 반영), `routeCoords` 우선순위가 `homeCenter`보다 앞섬 확인, 노선 선택 해제 시(`setRouteCoords(null)`) 인천FIR로 복귀하는 부수 효과도 의도에 부합, `{lat,lon}` 필드명 일관성(`fir.label`·`pollCenters`·`fetchAircraft` 전부 lat/lon) 확인. Critical/High 없음.
- [x] **요청 2**: "픽스에 마우스가 근처에 가면 호버 형태로 바로 지점과 위경도가 나오게." `frontend/js/layers/reference.js`의 `renderWaypoints()` 팝업(`bindPopup`, 클릭 필요)을 호버 툴팁(`bindTooltip`, `direction:"top"`)으로 교체 — `permanent:true`를 안 쓰므로 이 파일 §180 주석의 permanent 툴팁 성능 문제(줌 3~5·다량 마커 상시 DOM)와는 무관(마우스가 올라간 마커 1개에서만 DOM 생성).
  - `code-reviewer` 리뷰 통과 — `bindTooltip`은 호출 시점이 아니라 실제 hover 시에만 DOM 생성됨(Leaflet 동작) 확인, `groups.waypoints`를 클릭 팝업에 의존하는 다른 코드 없음 확인. Critical/High 없음.
- [x] `node --check`로 `layers/adsb.js`/`main.js`/`layers/reference.js` 구문 확인.

### 사용자 요청 반영 (2026-07-23) — 참조 데이터(공역·항공로·지점·SID/STAR) 정적 JSON → DB 전환 + 인천/김포 SID·STAR 결측 해소
- [x] **요청 배경**: "국내의 경우 경로 선택시 도착 star도 같이 나와야함" 제보를 조사한 결과 배선은 정상, 원인은 데이터 — 기존 `사전빌드_JSON/sidstar.json`(117건)에 인천(RKSI) SID가 없고 김포(RKSS)는 SID/STAR 전부 없었음(이식 패키지 자체의 데이터 한계). 마침 확보된 Jeppesen(ARINC 424 계열) 항행DB 원본 CSV 4종(`t_jepp_sid`/`t_jepp_star`/`t_jepp_waypoint_enroute`/`t_jepp_waypoint_terminal`)에는 인천·김포 모두 온전한 SID·STAR가 있어 이관하기로 결정, 사용자가 범위를 넓혀 참조 데이터 7종(FIR/TCA/항공로/공항/항행시설/픽스/SID·STAR) 전부를 정적 JSON에서 DB로 전환하기로 확정(플랜 파일: 승인된 계획 참고).
- [x] **Stage 0 구현**: `data-ingestion-backend/app/db/reference_tables.py`(10개 `reference_*` 테이블 스키마 단일 출처, `processed_*`와 달리 run_id/최신-run 윈도잉 없는 정적 마스터 데이터) + Alembic 리비전 `aee66ded869a`(테이블 생성 + `advisor_readonly` SELECT 권한부여). 적재는 업로드폼/skill_runner 파이프라인을 거치지 않는 1회성 스크립트 2개(하드코딩 금지 원칙상 API 부재 상태의 원본 CSV/JSON은 이 방식이 적합, 향후 API 확정 시 재검토): `scripts/migrate_static_reference_to_db.py`(기존 6종 JSON — FIR 247·TCA 63·항공로 89,555·공항 10,030·항행시설 8,056·픽스 58,812, 전부 원본과 건수 일치 확인) + `scripts/ingest_jepp_nav.py`(Jeppesen CSV 4종 — SID 1,517·STAR 1,702·엔루트지점 25,440·터미널지점 1,154, DMS→십진좌표 변환 포함, 변환 실패 0건). `app/config.py`에 `porting_package_root` 설정 추가(이 앱엔 없었음, 이관 스크립트에 필요).
- [x] **Stage 1 구현**: `backend/app/queries/reference.py`(신규) — 예전 `사전빌드_JSON` 행과 동일한 모양을 DB에서 조회해 반환(행 소스만 교체, 프로세스 생애주기 캐시 유지). SID/STAR는 fix_id→좌표 해석이 필요해(원본 CSV엔 좌표가 없음, ARINC 424 특성) 우선순위 조인 로직 신규 구현: ①해당 공항 터미널지점 ②동일 리전(ICAO 접두 2글자) 엔루트지점 ③navaid(동일 ident 여럿이면 공항 좌표에 최근접). `backend/app/reference/loader.py`는 `_load_json` 전체 제거, 행 출처만 `queries/reference.py` 호출로 교체(bbox 필터·도형 가공 로직은 무변경 — `load_airways`의 seq는 적재 시점에 미리 계산해 저장하므로 예전처럼 매 요청 재계산하지 않음). `backend/app/db/column_map.py`에 `REFERENCE_COLUMNS` 화이트리스트 추가(`tables.py`는 기존 리플렉션 로직 그대로 재사용, 무변경). `routers/reference.py`: 자산 에러(`_ASSET_ERRORS`) 대신 DB 에러(`OperationalError`/`DBAPIError`) 캐치로 교체, `envelope(..., source="reference-db")`. `backend/app/config.py`의 `porting_package_root`(더 이상 아무도 안 씀) 제거.
- [x] **실측 검증**: uvicorn 기동 후 `curl /api/reference/sidstar?airport=RKSI` → SID 76·STAR 43건(이전엔 STAR 4건뿐, SID 0건), `?airport=RKSS` → SID 34·STAR 24건(이전엔 0건) — 원래 제보한 결측 완전 해소. `firs?icao=RKRR`·`airports?icao=RKSI,RKSS`·`tca?bbox=...`·`navaids?bbox=...`·`airways?bbox=...` 전부 정상 응답 확인.
- [x] `senior-code-reviewer` 게이트 통과 — High 1건 발견·수정: `backend/app/config.py`에서 `porting_package_root`를 "아무도 안 씀"으로 판단해 제거했으나 `backend/batch/build_odr2.py`(ODR2 배치, `/api/routes*`가 서빙하는 산출물)가 여전히 그 필드를 참조해 다음 재실행 시 `AttributeError`로 죽는 회귀였음 — 필드를 다시 복원(이 서비스엔 여전히 필요한 설정값, 참조 로더만 DB로 전환됐을 뿐). `_load_dafif_lookups()`를 직접 호출해 크래시 없이 정상 동작함을 재확인(전체 `run()` 재실행은 프로덕션 `odr2.json` 아티팩트를 덮어써 이번 수정 범위를 벗어나므로 보류). Low 3건도 반영: ① `reference_tables.py`/마이그레이션 docstring의 "11개 테이블" 표기를 실제 10개로 정정 ② `queries/reference.py:fetch_sidstar`에 `airport` 파라미터 `.strip().upper()` 정규화 추가(다른 icao류 파라미터와 동일 관례, `?airport=rksi` 소문자도 정상 매칭 확인) ③ `ingest_jepp_nav.py:_load_procedure_rows`에 `_load_waypoint_rows`와 동일한 결측/형식오류 행 스킵 가드 추가(다음 AIRAC 주기 CSV 대비). SQL 인젝션·최소권한·에러메시지 비노출·두 적재 스크립트의 트랜잭션/멱등성은 문제없음 확인(리뷰 상세: fix 해석 커버리지 실측 SID 406/417·STAR 226/228 유지, navaid 중복 1462/8056건 중 최근접 판별이 실제로 작동함을 확인).
- [x] 리뷰 수정 후 백엔드 재기동 + 재검증: `?airport=RKSI`(119건)·`?airport=rksi`(119건, 소문자 정규화 확인)·`build_odr2._load_dafif_lookups()` 직접 호출(크래시 없음) 전부 정상.
- [x] 프론트 브라우저 확인(Playwright) — 인천(RKSI)·김포(RKSS) 각각 도착지 선택 후 지도에 SID(파랑 부채꼴, DEP 마커 주변)·STAR(녹색 부채꼴, ARR 마커 주변) 정상 표시 확인, `/api/reference/sidstar?airport=RKSI`(119건)·`?airport=RKSS`(58건) 프론트 실제 fetch 응답도 확인. 이전엔 두 공항 모두 이 레이어가 비어 있었음(원래 제보).
- [x] **원본 파일 backup 이동** — `/Users/sein/Desktop/project/t_jepp_*.csv` 4종을 `/Users/sein/Desktop/project/_db_migrated_backup/`로 이동(삭제는 보류, DB 검증 끝난 뒤 사용자가 판단). **`사전빌드_JSON/*.json`(FIR/TCA/항공로/공항/항행시설/픽스 6종 원본)은 이동하지 않음** — 두 가지 이유: ① `CLAUDE.md §0.1`이 `PORTING_PACKAGE_ROOT`(사전빌드_JSON 포함)를 읽기 전용 외부 자산으로 명시("수정·삭제하지 않는다"), ② `backend/batch/build_odr2.py`가 지금도 `firs.json`을 실시간으로 읽어 `/api/routes*`가 서빙하는 `odr2.json`을 만든다(리뷰에서 확인) — 옮기면 그 배치가 깨짐. `reference_*` DB 이관은 Stage 1의 `/api/reference/*` 응답 경로에서만 이 파일들을 대체했을 뿐, 원본 자체는 여전히 다른 곳에서 쓰인다.

### 사용자 요청 반영 (2026-07-23) — "수정코드 리뷰" 요청에 따른 ADS-B 후속 수정 2건
- [x] **배경**: 위 "항공기 아이콘 헤딩 회전 + 상시 2줄 라벨, 조회 반경 축소" 항목의 코드를 `senior-code-reviewer`로 다시 리뷰(사용자 요청), Medium 2건 발견 — 둘 다 사용자 확인 후 즉시 수정.
- [x] **수정 1(노선 표본 지점의 인덱스 기준 등간격 → 거리 기준 등간격)**: `frontend/js/layers/adsb.js`의 `sampleRoutePoints()`가 좌표 배열 인덱스로 등간격 샘플링해, 항공로 좌표가 촘촘한 공항 인근에 표본이 몰리고 성긴 대양 횡단 구간은 표본이 안 걸리는 문제(반경을 250→100NM로 줄이며 사각지대가 커질 위험) — `haversineNm()` 기반 누적 거리로 실제 지리적 거리 등간격 표본을 뽑도록 교체. 하드코딩돼 있던 `MAX_ROUTE_SAMPLE_POINTS`(모듈 상수)도 `CONFIG.adsb.routeSampleMaxPoints`로 이동(`config.js`/`config.example.json` 동기화, CLAUDE.md §2 하드코딩 금지 정합).
- [x] **수정 2(완전 장애 시 `poll()` 겹침 실행 경쟁조건)**: 모든 엔드포인트가 응답 없어 한 `poll()`이 `pollMs`(12초)보다 오래 걸리면 `setInterval`이 다음 `poll()`을 또 발사해, 겹쳐 실행된 `poll()`들의 `prune(seen)`이 서로 방금 추가한 마커를 지우는 경쟁조건이 있었음 — `pollInFlight` 불리언 가드 추가, 진행 중인 `poll()`이 있으면 새 tick은 건너뜀(`try/finally`로 항상 해제).
- [x] `node --check`로 `layers/adsb.js`/`config.js` 구문 확인, `config.example.json` JSON 파싱 확인.

### 사용자 요청 반영 (2026-07-23) — 경로 1(baseline) 대비 다른 옵션 갈라지는 구간 빨강 표시
- [x] **요청**: "경로추천의 경우 첫번째 경로 대비 두번째부터 달라지는 경로는 빨간색이든 다른색으로 구분해주면 쉽게 확인이 가능할듯." `route.js` 파일 헤더가 애초에 "이번 라운드 생략" 부가 연출로 뒀던 "최단 대비 추가 구간만 빨강" 세그먼트 diff를 이 요청으로 도입.
- [x] **구현**: `frontend/js/layers/route.js`에 `haversineNm()`(표준 haversine) + `isNearBaseline()`(baseline 좌표 배열과의 최소거리 판정) + `splitByBaseline()`(coords를 baseline 근접 여부가 바뀌는 지점마다 잘라 런 배열로 반환, 경계점은 양쪽 런에 공유해 선이 안 끊기게 함) 추가. `drawOption()`이 `baselineCoords` 인자를 새로 받아 — 없으면(경로 1 자신, 또는 baseline이 빈 배열인 방어적 케이스) 기존 단색 스타일 그대로, 있으면(경로 2+) `splitByBaseline`으로 나눠 근접 구간은 기존 파랑/회색 스타일·갈라지는 구간만 새 토큰 `CONFIG.tokens.routeDiffRed`(`#e03131`)로 그림. `render()`가 `options[0]`의 좌표를 baseline으로 1회 계산해 나머지 옵션에 전달(전체 겹쳐보기·특정 옵션 선택 두 분기 모두 적용). 이름 붙은 픽스 체인은 인천FIR 구간만 있어(`incheonTrackFixes`) 전체 노선 diff에는 못 쓰고, 레이더 트랙 좌표 근접도(`CONFIG.display.routeDiffThresholdNm`, 기본 20NM)로 근사 — 새 설정 2건(`display.routeDiffThresholdNm`·`tokens.routeDiffRed`)을 `config.js`/`config.example.json`에 동기화.
- [x] `code-reviewer` 리뷰 통과 — 근접 구간 스타일이 기존 단일 폴리라인 스타일과 완전히 동일해(경로 1과 갈라지지 않는 옵션엔 시각적 무변화 확인), `drawOption` 호출부가 `render()` 1곳뿐임을 grep으로 확인, config 양쪽 파일 동기화 확인, 날짜변경선 근처 두 시퀀스가 독립적으로 `unwrapLongitudes`돼 서로 다른 ±360 사본에 놓여도 `sin²(dLon/2+180°n)=sin²(dLon/2)`라 haversine이 영향받지 않음을 수학적으로 확인, `backend/batch/build_odr2.py` 확인 결과 트랙 좌표가 촘촘한 레이더 샘플이 아니라 정제된 픽스/플랜 토큰 기반이라 O(좌표수×baseline수) 스캔이 이 데이터 규모에서 성능 문제 없음 확인. Low 2건 반영: ① `baselineCoords`가 빈 배열(길이 0)인 방어적 케이스에 `!baselineCoords`만으로는 안 걸려 모든 다른 옵션이 전부 빨강으로 그려질 뻔한 것 → `baselineCoords.length === 0` 조건 추가. ② 마지막 좌표에서 근접/이탈이 딱 바뀌는 극단 케이스는 1점짜리 트레일링 런이 스킵돼 그 지점 색이 약간 어긋날 수 있음(시각적 끊김은 없음, 육안상 무시 가능한 코스메틱 수준이라 이번엔 보류).

### 사용자 요청 반영 (2026-07-23) — 그룹형 레이어 컨트롤(3섹션) + SID/STAR 분리 + 근접 경고(STCA)·동고도 조우 예측(CPA) 이식
- [x] **요청 배경**: 참고 완성본처럼 레이어 목록을 "기본 항공정보/절차·공역/실시간·기상" 3섹션으로 묶고, SID/STAR를 독립 체크박스로 분리, 유사호출부호·근접 경고(STCA)·동고도 조우 예측(CPA)·SUAS(특수공역 한국/세계)를 추가해달라는 요청. 조사 결과 SUAS는 원본 DB에 테이블 자체가 없고(원본 `공역.csv`는 있으나 미적재) 새 Stage 0 적재가 필요해 오래 걸림 — 사용자 확인 후 **이번 라운드는 프론트엔드 작업(그룹 UI·SID/STAR 분리·STCA·CPA)만 진행하고 SUAS는 별도 라운드로, 유사호출부호는 이번엔 제외**하기로 확정.
- [x] **구현 1(그룹형 레이어 컨트롤)**: `frontend/js/layer-control.js`(신규) — Leaflet 기본 `L.control.layers`가 섹션 헤더를 지원하지 않아(확인됨) `L.Control` 직접 확장. 체크박스 변경 시 `map.addLayer`/`removeLayer` 후 동일한 `overlayadd`/`overlayremove`를 수동 발생시켜 기존 SIGMET/PIREP/레이더의 온디맨드 조회 훅과 호환 유지. `main.js`가 이 컨트롤로 기존 `L.control.layers` 호출을 교체, 3섹션 구성(SUAS 2항목은 보류로 제외).
- [x] **구현 2(SID/STAR 분리)**: `frontend/js/layers/route.js`의 단일 `sidstar` 레이어그룹을 `sidGroup`/`starGroup`으로 분리(`proc` 필드로 이미 구분되던 데이터를 그대로 활용), 완성본과 동일하게 기본 꺼짐.
- [x] **구현 3(근접 경고 STCA·동고도 조우 예측 CPA)**: `frontend/js/layers/adsb.js`에 완성본 `detectStca()`(`완성본_전세계_항공로_지도.html:1170-1244`) 수치·수식을 그대로 이식한 `computeAlerts()` 추가 — FL180 초과 기체만 대상, 주의 10NM/5,000ft·경고 5NM/2,000ft, CPA는 상대위치·상대속도 벡터로 최근접시각(t) 계산해 15분 내·8NM·1,000ft 조건. 시각화는 마커 아이콘에 색상 링(경고=hazardRed, 주의=신규 토큰 stcaCaution, 선택된 CPA 쌍=신규 토큰 cpaPurple) + 근접 경고 쌍 연결선(`stcaLinesGroup`) + `#stca-panel` 목록(`renderAlertPanel()`, 콜사인 등 전부 escapeHtml)으로 구성 — 완성본의 canvas 단일 렌더와 달리 이 앱은 Leaflet 마커 기반이라 아이콘 재구성 방식으로 적응. 임계값·색상 전부 `CONFIG.adsb.stca`/`CONFIG.adsb.cpa`/`CONFIG.tokens`로 이동(하드코딩 금지). 체크박스 2개(`setStcaEnabled`/`setCpaEnabled`)는 `map.addLayer` 대상이 없는 계산형 오버레이라 `onChange`만으로 즉시 재계산·재렌더.
- [x] **구현 4(공항 온오프)**: `frontend/js/layers/reference.js`에 `airportsEnabled` 플래그 신설 — 공항은 줌 레벨로 `airportsLow`/`airportsAll`을 자체 교체하는 로직이라 단일 레이어가 아니므로 전용 토글 함수(`setAirportsEnabled`)로 노출.
- [x] `senior-code-reviewer` 리뷰 — High 1건 발견·수정: `showFocus()`/`showBulk()`(경로 선택·뷰모드 전환마다 호출)가 firs/airways/waypoints/navaids를 무조건 `addLayer`해, 사용자가 레이어 컨트롤 체크박스로 꺼둔 레이어가 뷰모드를 바꾸면 도로 켜지는 회귀 발견(처음엔 TCA만 개별 방어했으나 나머지 4개는 미반영) → `reference.js`에 `enabledByName` 맵 + `setGroupEnabled`/`isGroupEnabled`/`applyEnabledGroups`를 단일 출처로 신설, `showFocus`/`showBulk`와 `main.js`의 체크박스 `onChange`가 모두 이걸 참조하도록 통일(이전엔 체크박스가 `item.layer`로 직접 add/removeLayer해 `showFocus`/`showBulk`의 상태와 서로 몰랐음). `main.js`의 부팅 시 개별 addTo 루프도 제거하고 `reference.js` 생성자가 자체 초기화하도록 이동. CPA/STCA 수치·수식(완성본 대비 조건문 단위 대조)·XSS·하드코딩·SID/STAR 분리는 문제없음 확인. Medium 1건은 의도된 동작으로 문서화: STCA 연결선(`stcaLinesGroup`)은 완성본의 `show.STCA`/`show.AC` 독립 토글과 동일하게 "실시간 항공기 ADS-B" 체크박스와 무관하게 동작 — 주석으로 명시.
- [x] Playwright로 실제 브라우저 확인 — 그룹 컨트롤 구조·기본 체크 상태가 목업과 일치(`전 세계 FIR`✓·`TCA`✗·`항공로`✓·`항로 픽스`✓·`공항`✓·`항행시설`✓·`SID`✗·`STAR`✗·`ADS-B`✗·`STCA`✗·`CPA`✗·`SIGMET`✗·`PIREP`✗·`기상 레이더`✓) 스크린샷으로 확인, STCA/CPA 체크박스 토글 시 콘솔 에러 없음 확인, 리뷰 수정 후 "항공로" 체크박스를 끄고 노선을 선택해도 체크박스 상태가 유지됨(콘솔 에러 없음)을 재확인.
- [x] `node --check`로 `layers/adsb.js`/`layers/route.js`/`layers/reference.js`/`layer-control.js`/`main.js`/`config.js` 구문 확인, `config.example.json` JSON 파싱 확인.
- [ ] **보류(별도 라운드)**: SUAS 특수공역(한국/세계) — Stage 0 DB 적재(`공간데이터/공역.csv`, 16,186행, P/R/D/A/M/W/T 코드 체계) 완료 후 "절차·공역" 섹션에 추가. 유사호출부호 경고 — 사용자가 이번 라운드 범위에서 명시적으로 제외.
- [x] `node --check`로 `layers/route.js`/`config.js` 구문 확인, `config.example.json` JSON 파싱 확인.

### 사용자 요청 반영 (2026-07-24) — odr2/flow 파일 아티팩트 → 로컬 Docker DB 완전 정규화 통합

- [x] **요청 배경**: `backend/batch/build_odr2.py`(ODR2 경로추천)·`backend/batch/build_flow.py`(흐름관리 영향률, STEP A1) 둘 다 `processed_*`에서 집계한 결과를 이 서비스 소유의 **파일 아티팩트**(`app/reference/artifacts/{odr2,flow}.json` + `_meta.json`)로만 저장해 왔다(docs/02 §4.3에 "파일 아티팩트 방식"으로 명시적으로 결정돼 있던 부분). 사용자가 로컬 Docker로 띄운 Postgres를 실제로 쓰고 있어, 이 두 산출물을 완전 정규화된 DB 테이블로 옮기기로 확정(방식 3안 중 "완전 정규화" 사용자 확정).
- [x] **아키텍처 결정**: 새 테이블은 `processed_*`/`raw_*`/`reference_*`와 다른 네임스페이스(`advisor_` 접두사)로 이 두 배치가 소유하며, `processed_*`에는 여전히 아무것도 쓰지 않는다(읽기 전용 소비 원칙 그대로 유지). 마이그레이션(DDL)은 기존 관례대로 `data-ingestion-backend`의 Alembic이 소유(이 저장소는 마이그레이션을 실행하지 않는다는 기존 원칙, `advisor_readonly` role 자체도 이 방식으로 추가된 전례). 쓰기는 신규 역할 `advisor_artifact_writer`로만 하고 이 12개 테이블에만 GRANT — `processed_*`/`raw_*`/`reference_*`/`ingestion_*`에는 접근 권한 없음(최소권한, 과거 role 혼용 사고 이력 있어 특히 엄격히 분리). 기존 `advisor_readonly`는 신규 테이블에 SELECT만 추가로 받아 조회 경로는 지금과 같은 읽기전용 엔진을 그대로 쓴다. 서로게이트 id 없이 업무 키(dep/arr/rank 등) 합성 PK로 구성해 시퀀스 GRANT가 불필요하도록 설계.
- [x] **신규 스키마(완전 정규화, 12개 테이블)**: `advisor_odr2_{od,route,route_fir,route_fix,track_point,full_route_point}` 6종(경로옵션 9필드를 스칼라 컬럼 + FIR/픽스/좌표 자식 테이블로 분해) + `advisor_flow_{od,od_reason,od_limit,od_measure,od_hour,route_group}` 6종(흐름관리 영향 요약 스칼라 컬럼 + 주사유/주제한/조치ID/시간대영향률 자식 테이블 + 경로그룹 영향률). 스키마 단일 출처: `data-ingestion-backend/alembic/versions/d4f7a91c3e26_advisor_artifact_tables_odr2_flow.py`(DDL + role/GRANT) + `backend/app/db/column_map.py`(`ADVISOR_ODR2_COLUMNS`/`ADVISOR_FLOW_COLUMNS`, 조회·적재 양쪽이 공유하는 컬럼명 화이트리스트).
- [x] **코드 변경**: `backend/app/db/artifact_session.py`(신규 쓰기 엔진, `ADVISOR_ARTIFACT_DATABASE_URL` — API 프로세스는 참조하지 않으므로 미설정이어도 읽기전용 서버는 정상 기동) + `backend/app/config.py`(선택값 추가). `batch/{build_odr2,build_flow}.py`는 집계 로직(`aggregate`/`build_odr2`/`_match_affected`/`_od_aggregate`/`_route_group_aggregate` 등, 이미 바이트 단위 기준선 검증 완료된 부분)을 전혀 건드리지 않고, `run()` 마지막의 `json.dump`만 `_persist()`(TRUNCATE 후 INSERT, 트랜잭션 하나로 원자적 교체 — `reference_*` 정적 테이블과 동일한 truncate-and-reload 패턴)로 교체. `queries/{routes,flow_reasoning}.py`는 파일 로드(`_load`/`_cache`/`reset_cache`)를 제거하고 `db/tables.py` 리플렉션(기존 읽기전용 role) 기반 SELECT로 재작성, **반환 dict 모양은 예전과 동일하게 유지**해 `routers/routes.py`·프론트는 무변경. `routers/routes.py`는 에러 매핑만 파일 예외(`_ASSET_ERRORS`)에서 DB 예외(`OperationalError`/`DBAPIError` + 각 모듈의 `Odr2NotBuiltError`/`FlowNotBuiltError`)로 교체 — 같은 파일의 4번째 엔드포인트(`/api/routes/delay-history`, odhr, 별도 세션이 동시 작업 중)는 여전히 파일 기반이라 그대로 둠(의도적, 손대지 않음).
- [x] **동시편집 관측**: 이번 작업 도중 별도 세션이 같은 저장소에서 odhr(STEP A2)·참조데이터 DB전환·프론트 UX 다수·docs 11~14를 동시에 작업 중이었음(`git status`로 확인). 겹치는 파일 없음 — `routers/routes.py`에 손대지 않은 4번째 엔드포인트(odhr)가 작업 도중 추가된 것을 실측으로 확인하고 그 부분은 건드리지 않음. 2026-07-22/23에 이미 두 차례 있었던 패턴과 동일(전례대로 겹치지 않는 부분만 각자 진행).
- [x] **실측 검증**: `alembic upgrade head` 적용 후 실측 — `advisor_artifact_writer`로 `processed_flight_data` SELECT 시도 시 permission denied, `advisor_readonly`로 신규 테이블 INSERT 시도 시 permission denied(격리 확인). 배치 재실행(`python -m batch.build_odr2`/`build_flow`) 결과 OD 1,487개·경로그룹 3,083개(odr2), OD 664개·경로그룹 1,151개(flow) — 기존 파일 기준선과 동일 규모. **기존 `odr2.json`/`flow.json`(재실행 전 마지막 산출물)과 DB 조회 결과를 전수 비교**: odr2는 OD 1,487건 전부, flow는 OD·경로그룹 조합 1,038건 전부 **완전 일치(불일치 0건)**. uvicorn 실기동 + curl로 `/api/routes`(VHHH→RKSI 4옵션·743편, 기존 기준선과 동일)·`/api/routes/od-pairs`(1,487건)·`/api/routes/flow`(VHHH→RKSI impact_pct=23%·174편 영향, found:false 케이스 200 정상)·400/422/404 케이스 전부 확인. 검증용 uvicorn은 이미 떠 있던 다른 세션의 개발 서버(8088)를 건드리지 않도록 임시 포트(8189)에서 별도 기동 후 종료.
- [x] **실측 중 발견·수정(비결정성 버그)**: `od_pairs()`를 `total_flights DESC`로만 정렬해 편수 동률 그룹의 순서가 실행마다 달라질 수 있음을 발견(파일 아티팩트 시절엔 dict 삽입 순서로 우연히 안정적이었을 뿐, DB 정렬은 동률 내부 순서를 보장하지 않음 — `latest_run.py`/`build_odr2.py`에서 이미 겪은 동률 비결정성 함정과 동일 종류) → `dep, arr` 2차 정렬 추가, 두 번 연속 호출 결과 동일함을 재확인.
- [x] `senior-code-reviewer` 게이트 통과 — **High 1건 수정**: 읽기 측이 기본 격리수준(READ COMMITTED)으로 `routes_for()`/`flow_for()` 안에서 여러 SELECT를 순차 실행하고(od→route→fir→fix→track→frc), 라우터는 메인 조회와 `data_period()`를 완전히 별도 커넥션 2번으로 호출해, 그 사이에 배치의 TRUNCATE+INSERT가 커밋되면 한 응답 안에 신·구 세대 데이터가 섞이거나 `data`와 `meta.data_period`가 서로 다른 세대를 가리킬 수 있음(자동화 이후 발생 확률 ↑) → `queries/{routes,flow_reasoning}.py`에 `_repeatable_read_connect()`(REPEATABLE READ 격리) 추가, `od_pairs()`/`routes_for()`/`flow_for()`가 데이터와 `data_period`를 **하나의 트랜잭션·튜플 반환**으로 통합해 라우터의 별도 호출 자체를 없앰(`routers/routes.py`도 이에 맞춰 갱신). 수정 후 재검증(전수 일치 재확인, uvicorn 재기동 확인). **Medium 1건 수정**: 쓰기 측(`batch/{build_odr2,build_flow}.py`)이 `column_map.ADVISOR_ODR2_COLUMNS`/`ADVISOR_FLOW_COLUMNS`를 참조하지 않고 컬럼명을 `sa.column()`으로 손으로 다시 타이핑해, 조회 측(리플렉션 화이트리스트)과 달리 컬럼명이 어긋나도 조용히 있다가 INSERT 시점에야 raw 에러로 터지는 드리프트 방지 부재 → 두 배치 모두 `column_map`을 단일 출처로 참조하는 `_proxy()` 헬퍼로 교체, 재실행해 규모 동일함 재확인. **Low 1건 수정**: `build_flow.py`의 `_Flight`가 `build_odr2.py`의 ICAO 4자리 검증을 안 해 dep/dest 형식 이상 데이터가 `_persist()`의 OD 키 파싱(`split("|")`)을 깨뜨릴 여지 → 동일 가드 추가. **Low 1건은 보류(문서화만)**: `_ensure_built()`가 "배치 미실행"과 "배치 실행했지만 결과 0건"을 구분 못함 — 두 경우 모두 503이 합리적인 응답이라 기능상 문제는 아니고, 운영자 메시지 정확도 개선은 후속 과제로 남김. 트랜잭션 원자성(TRUNCATE+INSERT 단일 `engine.begin()`)·파라미터라이즈드 쿼리(문자열 조립 없음)·최소권한 role 분리(API 프로세스가 쓰기 엔진을 import하는 경로 없음)는 리뷰에서 문제없음으로 확인됨.
- [x] **원본 파일 정리**: 더 이상 읽지 않는 `backend/app/reference/artifacts/{odr2,flow,odr2_meta,flow_meta}.json` 4개 삭제(모두 `.gitignore` 대상 로컬 생성물, 커밋 이력에 없음 — 삭제해도 git에 영향 없음). 같은 디렉터리의 `odhr.json`/`odhr_meta.json`(다른 세션 소관, STEP A2)은 그대로 둠.
- [x] **문서 동기화**: docs/02-db-integration.md §4.3(저장소 결정 갱신)·§6(role 표에 `advisor_artifact_writer`/`ADVISOR_ARTIFACT_DATABASE_URL` 추가, GRANT SQL 예시) / docs/08-setup-and-dev-order.md(env 변수 표에 `ADVISOR_ARTIFACT_DATABASE_URL` 추가) / docs/13-ai-reasoning-dev-plan.md STEP A1(파일→DB 갱신 note) / docs/07-checklist.md(이 절 + 기존 Stage1·Phase A 항목에 갱신 note) 전부 반영.
